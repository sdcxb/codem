# 当前缺口清单（唯一一份）

> **本文是 mimo-gui / codem 当前状态的唯一清单。** 第 72 轮（2026-09）起维护。
>
> `docs/` 下还有上百份历史文档，其中 40 份文件名看起来就像"当前待办/当前缺口"
> （`TODO.md`、`AUDIT-ZERO-GAP.md`、`IMPLEMENTATION-PLAN-FULL.md`、`WECODE-REF-GAP-ANALYSIS.md` …）。
> 它们**都已经加了「历史文档（不再维护）」横幅**，并且有一条门禁用机器保证这件事：
> `src/test/docs-current-gap-list.test.ts`。理由很具体：实测对 `TODO.md` 读出来的"还没做"里，
> 有相当一部分早就做完了（它列的 `InlineMessageEdit` / `ScrollbarMarkers` /
> `ScrollToBottomIndicator` 都已在代码里存在），于是"缺口清单"自己变成了谣言来源。
>
> **要判断"还有没有缺口"，只看本文。** 本文里每一条都要写清：**判据是什么、怎么量、量到多少**。

---

## 一、机器状态（每次发布后更新）

| 项 | 值 | 怎么核对的 |
| --- | --- | --- |
| 装机版（用户手里那份） | **1.16.181** | 真机启动后读 `%LOCALAPPDATA%\Codem\codem.exe` 的 ProductVersion。**1.16.149 起三次更新器自升（150/151/152 都是它自己升的）**；153 起各版是**静默直装构建产物**（`Codem_x.y.z_x64-setup.exe /S`，因为要立刻复核读数，不等 CDN 同步；各版 `latest.json` 远端 7/7 都已发） |
| **第 181 轮（装机版 1.16.181，全面审计第 1 轮 + 整改）** | **按"功能 / 性能 / UI / 内存"四路审计**（计划里的 P2 全部做完后开始）。**功能**：全量用例 + 逐面板走查（142 入口 / 护栏拒绝 32，第二轮 34）⇒ 四类真缺陷**全为 0**（控制台 error 0 / 无名图标按钮 0 / 真控件被真控件挡 0 / 间距例外不成立 0）。**UI**：`scan-ui` error 0 / warn 0、`css-contract` 无意外变化、12 个面板逐个开关正常。**内存**：反复开合 12 轮 + 强制 GC 逐轮采节点与堆 ⇒ 后半段 **Δ0**（无泄漏），但**首次打开面板后 DOM 从 620 涨到 10901**。**性能**：`bench:mem` **156MB** 峰值 / 5.2s（390MB 正文；两次读数 156/170 的差是运行间抖动，非回归）、`bench:db` 10 万行写入 7.1s。**抓到的真问题（唯一一个）**：逐个面板**先重载到干净状态**再量 ⇒ 插件管理自身 **6153 节点**（其余 620/321/228/197/151），而干净状态的整个应用只有 620 ⇒ 点开一次 DOM 变 11 倍；归因到 `.skill-market-grid` 一棵子树 **6095** 节点 = **208 张卡**（207 内置插件 + 1）× 约 29 节点/张（全页 1952 个内联 `<svg>` 全来自它们），根因是 `plugins.map(...)` 一次性全渲染。**处置**：新增共享原语 `src/components/ui/IncrementalList.tsx`（首屏 40 + 哨兵进视口追加 + **可聚焦的「再显示 N 项」按钮** + `resetKey` 变化回到首屏片数），覆盖 4 处同型网格。**装机版复核（改前 → 改后）**：插件管理自身节点 **6153 → 1196（5.1×）**；首屏卡片 **40**（不是 208）、按钮文案「再显示 40 个插件（还有 168 个）」且是 `<button>`；五次追加 80/120/160/200/**208** ⇒ **分片没变成截断**，208 张卡名字全唯一、与侧栏「全部 208」三面对上；控制台 **error/exception 0**；内存复审计峰值从 10901 降到 **2561**、后半段 Δ 为负。**两只仪器虫记档**：① 第一版用 `Performance.getMetrics` 数节点，而 `Page.reload` 会重置该域 ⇒ 每项读成 **-1**、表里全是 0、结论"每个面板都不占节点"（**"全是 0"最危险**），改成页内数节点且无效值**抛错不当 0**；② 用例 IL-2 第一版点的是**测试夹具自己的按钮**、IL-8 第一版只查"文件里有没有 `IncrementalList` 字样" ⇒ 两条变异都漏过，改成判据落在被测对象上。用例 **IL-1…IL-8**（IL-8 为源级判据）+ 变异 **50/50 红**。**功能端到端（真回合）**：发一条消息后查两侧 —— `messages` 里 `role=user id=user-1790442745576`、`session_events` 里 `seq=9294 user_message messageId=user-1790442745576`（**同一个 messageId**）、**文本事件完全重复 0 条**、其它会话也都是 0 ⇒ **O-31 去重没有碰坏写入路径**。**如实标注**：这一轮**没有拿到"助手回复"** —— 应用配置是 `mode=cli` 且 `mimoPath=""`，LLM 调用报 `Failed to fetch`；判别它是**环境条件**而不是产品缺陷的依据是：① 在**页面里**直接请求 `https://api.deepseek.com` 得到 **HTTP 401**（端点可达，只是没带凭据）；② 本轮改动全在 UI/存储侧，不经过 fetch 路径；③ 应用在传输失败时**如实降级**（界面给出可读错误 + 自动重试提示 + 停止入口），没有静默挂死。`verify` **408 文件 / 6366 通过**、`audit` 20 道 exit 0 | 复跑 `npm run verify` / `node .preview-shot/_audit-dom-budget2.mjs`（逐面板节点，需 CDP）/ `node .preview-shot/_verify-181-incremental.mjs`（可达性）/ `node .preview-shot/_verify-181-turn-record.mjs`（真回合的入库读数）/ `node .preview-shot/mutate-p0-gates.mjs`（50/50） |
| **第 180 轮（装机版 1.16.180，P2-6 收口：浮层挂载入口）** | `createPortal` 手写 **43 处 / 21 个生产文件**，逐个数过容器实参 —— **21 处全是 `document.body`**（只有 `SelectionTooltip` 是 `containerRef.current \|\| document.body`）。代价是三条具体的：容器策略散在 21 个文件里、门禁**无法回答**"新浮层有没有走共享层"（共享层不存在）、无 DOM 环境要各自判断而今天没人判断。**处置**：新增 `src/components/ui/portal.ts` 作为唯一挂载入口（从 react-dom 取 createPortal，全项目只此一处；默认 `document.body`；需要别的宿主显式传 `container`；**没有 `document` 时安全返回 null**），21 个文件改导入源、**21 处冗余 `document.body` 实参删除**、1 处自定义宿主保留。门禁 **PORTAL-1a / PORTAL-1b**（后者用**字符扫描配对括号**，正则数括号必然数错）。**装机版复核**：搜索浮层 `role=dialog aria-modal=true`、挂成 `document.body` 直接子节点、Escape 可关；6 个面板开合正常、关掉后 body 只剩 `#root` + `scrollbar-markers-rail`（无泄漏浮层）；控制台 **error/exception 0 / warning 0** | 复跑 `npm run verify` / `node .preview-shot/_verify-180-portal.mjs`（需 CDP） |
| **第 179 轮（装机版 1.16.179，P2-1 收口 + O-32）** | 空态棘轮 **24 → 1**（唯一例外是 ChatPanel 欢迎页 `.empty-state`，理由写进门禁）。按**真实标记形状**分族后补两个修饰：`.empty-hint.is-stacked`（图标在上，间距走新令牌 `--empty-hint-gap` = `var(--space-4)`）、`.empty-hint.is-fill`（`flex: 1`）。**装机版运行时真值**（`.preview-shot/_probe-shared-classes.mjs`，注入式测量、不动数据）：9 种共享类写法 → 内边距**只有 2 个取值**（`24px 24px 24px 24px` / `8px 6px 8px 6px`）、字号只有 2 个（13px / 12px）；`--empty-hint-gap` 实测 **8px**（1.16.178 里**未定义** —— 这是"新令牌真的上线了"的对照）；`is-stacked` → `display:flex` + `gap:8px`；`is-fill` → `flexGrow:1`；`.empty-state` 是 16/16 + 14px + `flexGrow:1`（确实不是一类东西）。**规格值对照**（`_diff-empty-before-after.mjs`，24 处逐条）：24 套内边距 → 2 个令牌（`--empty-hint-pad` / `--empty-hint-pad-compact` 取值未变）。**顺带做掉"第三族"（O-32）**：名字像空态却不含 `empty-hint` 的 37 处里，**9 处真空态**（它们不写 `padding`，棘轮从来没看见）全部迁入，其余 **15 个 token 分三类登记**。**四个坑记档**：COND-1 排除条件按子串 `input` 吞掉 `.input-popover-empty`（真值 1→2）；`count()` 按子串数不到组合写法；`.kg-empty` 有两条规则只迁一条（被 LEGACY 断言抓到）；迁移脚本写错 4 条规则的文件且不兼容单行/多行。**装机版复核**：维护汇总行 `文本事件去重 0 条（本库已做过）`、`完整性检查 通过`、不变量审计仍是**历史缺口 704 / 本次新产生 0**、控制台 **error/exception 0**（2 条 warning 是 `Page.reload` 自身的 IPC 重载痕迹）。门禁 COND-1 扩到 4 修饰 + **新增 COND-1b**（名字像空态者必须挂 `empty-hint` 或在登记表里写明理由 + 扫描量 > 80 防空集）+ 逐站点自证 28 项（**它真的抓到过 `McpMarketplace` 漏挂，而当时所有门禁全绿**）；变异 **43/43 红 + 还原逐字节回绿**；`verify` **407 文件 / 6355 通过**、`audit` 20 道 exit 0、`scan-ui` error 0 / warn 0 | 复跑 `npm run verify` / `node .preview-shot/mutate-p0-gates.mjs`（43/43）/ `node .preview-shot/_probe-shared-classes.mjs`（需应用带 CDP 启动）/ `node .preview-shot/_diff-empty-before-after.mjs` |
| **第 178 轮（装机版 1.16.178，O-31：清掉历史重复文本事件）** | 引擎新增 `events.dedup_text`（**默认干跑**、范围刻意只有 `user_message`/`assistant_text`、判据是**载荷逐字节相同**、**保留 `MIN(seq)`**、`measure_delete_impact` 报账 + `storage_audit` 触发器留痕、幂等）+ 读路兜底 `collapseExactDuplicateTextEvents` 接进 `session_event_search` + 启动维护一次性步骤（`settings` 标记 + advisory + 汇总行）。**真机（只查副本）改前 → 改后**：文本事件 **6327 → 258**、不同载荷 258、**完全重复 6069 → 0**、非文本事件 **2098 → 2098（一条没动）**；**回放等价性：逐会话比对投影消息 253 条、不一致 0 个**；干跑读数与独立探针**逐项一致**。**装机版实测（1.16.178）**：真库 **8425 → 2356 事件、文本 6327 → 258**；维护汇总行首跑 `文本事件去重 6069 条`、**重载后 `文本事件去重 0 条（本库已做过）`**（一次性标记生效）；异常提示条 `persist-alert is-advisory` 原文含「清理了 6069 条完全重复的文本事件（删除前 6327 行里只有 258 条是不同的）」；控制台 **error/exception 0**（3 条 warning = 1 条本轮 advisory + 2 条 `Page.reload` 自身的 IPC 重载痕迹）；不变量审计仍是**历史缺口 704 条、本次新产生 0 条**。**两处口径被推翻并更正**：① 旧探针 `events.list { limit: 5000 }` 把最大会话截断在 5000 行 ⇒ 原文的 5853/5614/4368 全部偏小（真值 6327/6069/4842，算术自证 4368+474=4842）；② 原计划的「保留 seq 最大（最新）」**是错的** —— 投影里 `user_message` 首次胜出、`assistant_text` 位置也取首次，保留最新会静默改变回放顺序，正确规则是保留 `MIN(seq)`（交错流用例 + 变异 M1 钉住）。用例 Rust 8 条 + TS 9 条、变异 **8/8 红 + 还原逐字节回绿**。`verify` **407 文件 / 6355 通过**、`audit` 20 道 exit 0、`scan-ui` error 0 / warn 0 | 复跑 `npm run verify` / `node .preview-shot/mutate-o31-dedup.mjs` / `node .preview-shot/_verify-o31-dedup.mjs`（副本上的改前改后 + 回放等价性）/ `node .preview-shot/_count-dup-text-events2.mjs`（**别再用旧探针的汇总数，它有 `limit:5000` 截断**） |
| **第 177 轮（装机版 1.16.177，P2-2 收尾：对话框语义）** | 26 个对话框本体补 `role="dialog"`（确认类用 `alertdialog`）+ `aria-modal="true"` + **取自组件自己可见标题**的可访问名；遮罩**刻意不加**语义。实测 `role="dialog"` 4→**25**、`alertdialog` 1→**4**、`aria-modal` 4→**28**、语义不全 **18/26 → 0/26**；门禁 **A11Y-DLG-1**（含"扫描到的本体不得少于 20 个"防空集假绿）。**装机版复核**：产物 JS 里 `role:"dialog"` **27** 处 / `role:"alertdialog"` **5** 处 / `aria-modal` **29** 处；CDP 打开搜索对话框读到 `role=dialog aria-modal=true aria-label=搜索`，**AX 树里 `role=dialog name="搜索" modal=true`**（语义真的生效，不只是"源码写了属性"）。**如实记两次口径错**：① 把 `.modal-overlay` 遮罩也算成"该有 dialog 语义" ⇒ 永远报 31 处；② 用**前缀**匹配 `nb-dialog` 把 `-overlay/-header/-title/-close` 全算进来 ⇒ 报 77 个"盒子" | 复跑 `npm run verify` / `node .preview-shot/_measure-dialog-boxes.mjs` / `node .preview-shot/_verify-177-dialog-ax.mjs`（需应用带 CDP 启动） |
| **第 166 轮（装机版 1.16.166，P0：层次/状态/节奏 + 悬空逗号）** | 按 `docs/SIDEBAR-AUDIT-OPENBITFUN.md` 的 P0 施工：①内容做成浮起纸面（左两角 24px + 左缘阴影 + 壳体改 `--chrome-surface` —— **壳与纸面必须不同色**才看得见层次）；②删掉 1.16.164 那条「同色低 α 覆盖」（实测 hover **0% 面积变化**、且压过 `.active` ⇒ 选中一碰就没），改墨色 `--row-hover`/`--row-pressed` + `:not(.active)` + 按下态 + 次级文字色 + `--transition-color`；③行高 40.33/31.5/38 → **30px** 常量、分组标题 24px、容器与行内边距统一、行间距 2px；④修 `styles.css:762` 悬空逗号（浮层 `transform-origin` 全 center，与 D-4 相反）。新增门禁 SHELL-1 / RHYTHM-1、改判据 NATIVE-2；变异 **7/7 全红 + 还原回绿**；`verify` exit 0（403 文件 / 6315 通过）、`audit` exit 0、`scan-ui` error 0 / warn 0 | |
| **第 165 轮（装机版 1.16.165，P2-1 的另一半 + 变异自证补齐）** | 高对比档此前只把玻璃降级成实色、**没有任何令牌级覆盖**（文字/边框/焦点环一个像素没变），而唯一相关的 APPEARANCE-5 只断言 `.sidebar` 存在 ⇒ 半成品全绿。补齐：`html[data-contrast="high"]` 覆盖文字两档/边框四档/焦点环两项（一套值管两档：文字是「墨→纸」混合，N 调大在两档都是「远离底色」）；实测 `--text-muted` 5.28→8.75:1、`--text-secondary` 7.37→12.30:1、边框可见度 1.19→1.73:1、焦点环 2px→3px。为量半透明边框可见度，给检查器新增 α 感知解析（老 `parseRgba` 连 `rgb(31 31 30 / 9%)` 都认不出）+ 门禁 ALPHA-1…4（含「color-mix 必须预乘 α」的规范参照值）。覆盖盘点：P1-3/D3/D4 的阴影断言此前**无变异自证** ⇒ 新增 S1–S5 + A7–A11，全部被抓、还原逐字节回绿；本项变异自证合计 **62 条**。**如实标注**：P1-1 的「结构翻转（灰工作区+白卡）」不做（灰面占屏比 32.8% vs 参考 5.5%，方向相反）。实测 `verify` exit 0（403 文件 / 6313 通过）、`audit` exit 0 | |
| **第 164 轮（装机版 1.16.164，产物口径对齐 / SKIN-2）** | 装机版复核逮到：**压缩器会把命名色 `white` 改写成 `#fff`** ⇒ 1.16.163 的产物里 Dream 皮肤规则体仍有 2 处颜色字面量（`.btn-send/.send-button`、`.inline-diff-btn.accept`），而源码门禁 SKIN-1 只看 hex/rgb/rgba、看不见命名色。处置：改用同值令牌 `var(--dream-c7)`（=#ffffff，零视觉变化）+ 新增门禁 **SKIN-2**（规则体禁命名色，CSS Color 4 全表；豁免 `transparent`/`currentColor`，压缩器对它们逐字保留）+ SKIN-1/SKIN-2 共用一套区间口径。产物实测 `dist/assets/main-Cucc8UK7.css`：字面量 hub 0/dream 0、命名色 hub 0/dream 0。实测 `verify` exit 0（402 文件 / 6308 通过）、`audit` exit 0、23 条变异全红 + 还原逐字节回绿 | |
| **第 163 轮（装机版 1.16.163，P2-3 皮肤数据化）** | 两套皮肤规则体里的裸颜色**归零**：Hub 89 处 / Dream 112 处 ⇒ 收进各自顶部的调色板令牌块（新增 `--hub-cN` 63 个、`--dream-cN` 81 个；与既有令牌等值的 38 处直接复用）。**零视觉变化是机证的**：CSS-INTEGRITY-7（2733 个类的生效取值快照）不加 `--write` 直接通过、`css-contract --check` 报 ✅无变化。新增门禁 **SKIN-1** + 变异 SKIN1；棘轮收紧 `color` 140→17、`box-shadow` 29→8。如实记两坑：迁移脚本第一版会删光注释（改用原文替换 + 注释区间跳过）、SKIN-1 因 CRLF 偏移算错报 4 处假红。实测 `verify` exit 0（402 文件 / 6307 通过）、`audit` exit 0 | |
| **第 162 轮（装机版 1.16.162，P1-1 表面角色 + D8 身份色）** | ①`--surface-raised`（= `var(--bg-primary)`，零视觉变化）把卡片面与画布面解耦并接上 4 处真实消费方；**不新增 `--surface-subtle`**（已有 `--surface-1/2`，避免两套浅面阶梯）。②身份色只做**有消费方**的执行模式徽标（`--identity-mode-workspace/-worktree`，含 14% 同色浅底），头像/工具族**评估后不做**并写明理由。③新增 3 条用例 + 变异 **21/21**；如实记两处变异脚本自身问题（M18 判据缩进、M19/M20 挂错套件）。实测 `verify` exit 0（402 文件 / 6306 通过）、`audit` exit 0 | |
| **第 161 轮（装机版 1.16.161，颜色用量注册表 audit 第 20 道）** | 新增 `tools/audit/scan-color-roles.mjs`（三层：登记制 / 实算对比度 / unresolved 预算棘轮），第一次跑就抓出 **9 处真实违规**并全部修掉：白字压暗色状态色块 2.54/2.52（8 处 ⇒ 新增 `--text-on-status`）、`--error` 文字压悬停面 4.14、`--accent` 文字压内嵌块/悬停面 3.92/3.48、`.quote-context-banner` 5.99、`.sp-toggle` 死声明白字 1.16（直接删）。**门禁自己踩的三个坑也都修了并进了变异**：亮色档整档没被检查（选择器空格 vs 换行）、报出的行号错位几百行（去注释吃偏移）、半透明表面漏了预乘还原（本仓库第三次）。新增 7 条用例 + **变异 6/6**；`audit` 现为 **20 道**；实测 `verify` exit 0（402 文件 / 6302 通过）、`audit` exit 0。**如实标注**：暗色白字压品牌紫 3.99 按 AA-large 的 3 登记（要过 4.5 需改紫或改深色字，属产品决策） | |
| **第 160 轮（装机版 1.16.160，外观档位启动路径）** | 复核 1.16.159 时实测 `data-contrast`/`data-density` 都是 `null` ⇒ 查出 `applyAppearanceAttributes()`（DB→镜像→默认 那条链）**在产品代码里没人调**（只有测试在调）——"改了档但镜像丢了"（清缓存/换机恢复设置）会一直停在默认档，直到再碰一次设置页。**修法**：`main.tsx` 首帧前按镜像应用 + `TitleBar`（DB 就绪后）用 `getSetting` 校正；新增 **APPEARANCE-6** 钉住两处都在（且 DB 那次必须传 `getSetting`）；变异扩到 **7/7 红**。实测 `verify` exit 0（401 文件 / 6295 通过）、`audit` exit 0 | |
| **第 159 轮收官核对（装机版 1.16.159，外观档位接线 + 令牌解耦）** | 全量 **401 文件 / 6294 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**；`npm run audit` **exit 0**；变异 **6/6（外观档位）+ 16/16（样式令牌）**。**查到并补上的真实缺口**：`[data-contrast="high"]` 的 CSS 从 1.16.156 起就有，但**全项目没有任何写入方** ⇒ 规则是死的、用户永远看不到，而 CSS 侧门禁全绿（只问"规则在不在"）。现在：设置页外观页签有「对比度」「界面密度」两个下拉、`index.html` 首帧前读镜像、`appearance-modes.ts` 是唯一真相源（DB→镜像→默认）。**令牌解耦实测**（`.preview-shot/_text-ramp-delta.mjs`，逐通道）：浅色 `--text-primary/secondary` 差 **0**、`--text-muted` 差 **3**；暗色 **≤1**；hub 皮肤 **0**；`--accent-muted` 暗色/hub 差 **0**、浅色差 **7**（原来是另一个紫）——即解耦后观感不变。**仪器教训（如实记）**：完整 `verify` 里 `coverage-per-file` 报 `src/core/issue/issue.ts functions 42.1% < 地板 46%`，而**该文件这一轮一行没动**；重跑一次即通过（`按文件地板闸门通过：307 个文件都在地板之上`）⇒ 函数级覆盖会随 vitest 的 worker 分片与动态 import 时序抖动，**遇这条红先重跑一次再判**，别急着改地板。**发布侧**：tag `v1.16.159`、远端清单与资产随后校验 | 复跑 `npm run verify` / `npm run audit`；外观档位复量：`node .preview-shot/mutate-appearance-gates.mjs`（6/6 红）、`node .preview-shot/_text-ramp-delta.mjs` |
| **第 158 轮收官核对（装机版 1.16.158，系统窗口材质档）** | 全量 **400 文件 / 6287 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**；`npm run audit` **exit 0**；变异 **15/15（玻璃/按下态/系统材质档）**。**装机版实测**（`_verify-1158-material.mjs`）：`data-native-material=sidebar`、`data-native-material-kind=**mica**`（本机 Win11 25H2、透明效果已开）；`html`/`.app`/标题栏背景 `rgba(0,0,0,0)`；侧栏 `color(srgb .984 .984 .980 / 0.88)` + `backdrop-filter: none`；**主内容面仍是 `rgb(255,255,255)` 不透明**（壁纸透不进正文）。**A/B 客观读数**（同一次会话里摘掉属性再截一张，`_material-ab.mjs`）：标题栏中段 rgb(238,239,241) → rgb(234,231,255)（**Δ16.6**）、侧栏空白带 Δ**7.1**、主内容面 Δ1.4（≈0）⇒ 外壳真的换成了系统材质、正文一点没被波及。**与对方的像素对照**（同一套统计，`_obf-screenshot-stats.mjs`）：对方官方截图侧栏区 rgb(242,242,243) / 内容 rgb(249,249,249)（差 7）；我们侧栏 rgb(242,241,243) / 内容 rgb(246,246,246)（差 4）⇒ **对方也不是"看穿"，只比内容暗 7 个色阶**。**发布侧**：tag `v1.16.158`、远端清单与资产随后校验 | 复跑 `npm run verify` / `npm run audit`；材质档复量：`node .preview-shot/_verify-1158-material.mjs` + `node .preview-shot/_material-ab.mjs`（需应用带 CDP 启动） |
| **第 157 轮收官核对（装机版 1.16.157，玻璃可见化 + 阴影阶梯 + 状态色四角色）** | 全量 **400 文件 / 6284 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**；`npm run audit` **exit 0**；变异 **12/12（玻璃）+ 13/13（样式）**。**装机版实测**（`_verify-1156-installed.mjs` + `_verify-1157-tokens.mjs`，全部取运行中页面的计算样式）：侧栏 `color(srgb 0.984 0.984 0.980 / 0.62)`（= **62%** 玻璃，底色 `#fbfbfa`）+ `blur(12px) saturate(1.2)`；`.app` 的 `background-image` 确实是场景层（`radial-gradient(1100px 620px at 0% 0%, color(srgb 0.396 0.333 0.878 / 0.16) …)`）；浮层 94% + 同档模糊；高对比档下侧栏回 `rgb(251,251,250)` + 无模糊（撤掉属性回 62%）；`--shadow-md/lg/popover` 已解析成**阶梯别名**（第 2/4/3 档，全中性）、`--shadow-glow` 派生自 `#6555e0`、`--error-content` = `color-mix(in srgb, #cf222e 86%, #1f1f1e)`、`--error-surface` 解析为 `color(srgb 0.81 0.13 0.18 / 0.1)`。**玻璃"看得见"的客观读数**（同会话 A/B 截图像素统计）：侧栏上段 浅色 rgb(243,242,244) → **rgb(229,226,246)**（Δ21.5）、暗色 rgb(25,27,29) → **rgb(42,41,67)**（Δ43.9）；下段几乎不变（Δ3.2 / 7.8）⇒ 顶部有一层渐隐薄雾。**装机版截图复核**：`_shot-light-1157.png` 的上段读数与"出包前注入预览"**逐位一致**（rgb(229,226,246) / rgb(42,41,67)）⇒ 预览与出货一致。**发布侧**：tag `v1.16.157`、远端清单与资产随后校验 | 复跑 `npm run verify` / `npm run audit` / `node .preview-shot/_verify-1156-installed.mjs`（需应用带 CDP 启动）；玻璃可见度复量：`node .preview-shot/_glass-region-stats.mjs <png>` |
| **第 156 轮收官核对（装机版 1.16.156，玻璃/按下态/禁用态/品牌色阶梯）** | 全量 **400 文件 / 6278 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**（按文件地板 307、knip 棘轮 94/59/10、jscpd 171）；`npm run audit` **exit 0**（含新增的令牌卫生 H5）。变异自证 **9/9 + 10/10**。**装机版实测**（`node .preview-shot/_verify-1156-installed.mjs`，全部从**运行中的页面**取 getComputedStyle，不看源码文本）：侧栏 `backdrop-filter: blur(12px) saturate(1.2)`、底色 `color(srgb 0.9725 0.9725 0.9686 / 0.9)`（= 90% 玻璃）、`box-shadow: inset 0 1px rgba(255,255,255,.08)`（内高光）；实例化的 `.popover-shell` 与 `.model-picker` 都是 94% + 同档模糊（`.model-picker` 改前是实色 `--bg-secondary`）；一个**真实的 disabled 控件** opacity = **0.5**（= `--opacity-disabled`）；`--blur-base` 已不存在（零消费方已删）；品牌色阶梯解析成 `color(srgb 0.396 0.333 0.878 / 0.08)` 与 `/ 0.3`；**高对比档**：侧栏回 `rgb(248,248,247)` + `backdrop-filter: none` + 右边框 9%，浮层回 `rgb(255,255,255)`，撤掉属性后回到 90% 玻璃。**暗色读数**（`_probe-dark-1156.mjs`）：侧栏 `color(srgb 0.102 0.110 0.110 / 0.9)` + 同档模糊 + 内高光；按下态 `rgba(255,255,255,.14)` 压在画布上合成 `rgb(48,49,49)`（L=**0.0300**）> 悬停面 L=**0.0256** ⇒ 方向与幅度都对（**与离线脚本算的 0.0300 逐位一致，两套算法互证**）。**仪器教训（如实记，这一轮又踩了一次"把仪器假象当缺陷"）**：第一次量高对比档时在**同一条任务里**读 `getComputedStyle`，拿到的是 `oklab(… / 0.9)` —— **过渡插值中间态**，差点误报"高对比档没生效"；侧栏上有 `transition: background-color .2s`，量状态切换必须**等过渡结束**（`_probe-high-contrast2.mjs`，已写进脚本注释）。**截图**：`.preview-shot/_shot-light-1156.png` / `_shot-dark-1156.png`（这一轮的观感改动以用户眼睛为准；模型读不了图，程序化读数见上）。**发布侧**：tag `v1.16.156` → `cfb5493`（`verify-release-tags` 5 条 ✅ 全一致，无新增例外）、远端清单 **7/7**（含远端资产 sha256 与签名校验）、资产 5 个（nsis + sig、msi + sig、latest.json） | 复跑 `npm run verify` / `npm run audit` / `node .preview-shot/_verify-1156-installed.mjs`（需应用带 CDP 启动）；装机版版本号见左列 |
| **第 154 轮收官核对（装机版 1.16.154）** | 全量 **399 文件 / 6248 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**（两次连跑都绿：按文件地板 307、knip 棘轮 94/59/10、jscpd 169）；`npm run audit` **17 道 exit 0**（含本轮修好的语法门禁与正则 Unicode 门禁的一次偶发假红）。**未关闭缺口 2 项**：`O-1`（渲染进程崩溃根因，事件驱动）+ `O-29`（重载后重复写事件，本轮复核时新发现）。**装机版实测（用户要的那条判据）**：装好 1.16.154 后跑真回合（主聊天一次工具回合产出 **4 条空正文纯工具轮助手行**，其 `tool_call`/`tool_result` 事件的 `messageId` **逐条对得上行 id**；再跑一次委派回合走 `executeSessionTurn`），**重载页面让启动维护重跑** ⇒ `[不变量水位] 上次水位 1102 键、本次存在 704 个、**本次新产生 0 个**`（`检查 8 个会话`）、无 `[Advisory] maintenance.invariantAudit.new`。**口径修好的算术证据**：修前那一轮 `本次存在 707 个`（706 + 当天新增 1 条 `err-…`），707 − 3（库里 3 条 `system` 行）= 现在这 **704**。**「有没有做完没进包的功能」也逐项查过**（三条独立判据）：①构建之后产品源码**零改动**（mtime 扫 `src/` 非测试 + `src-tauri/src/`，只有测试/工具/文档四类变了，而它们不进包：`frontendDist=../dist`、无 `resources`、安装目录只有 `codem.exe`+`uninstall.exe`）；②装机版**运行中**加载的主包 sha256 与本地 `dist` **逐字节一致**（且含本轮标志物、不含改前那句双写）；③装机版 `codem.exe` 里有本轮 Rust 标志物，与本地产物只差打包器写的 3 字节 bundle-type 标记 ⇒ **产品代码零遗漏，无需新版本号**。同一轮还查出**发布侧的账**没对上（本地领先远端 64 个提交、`v1.16.150…154` 五个 release 的 tag 全指向旧提交 `b4ae90d`）并已修：推送 master、把五个 tag 移到各版真实提交、五条 release 说明各补一段 tag 校正；**资产未动**（远端清单仍 7/7、`releases/latest` 仍是 v1.16.154）。**如实标注**：这两轮复核走的是主聊天 + 委派（同一套 `executeSessionTurn` 代码），**没有**再让用户从手机发微信；微信端到端读数沿用 1.16.152 那次。这一轮**不是只读钻取**（故意跑了真回合）⇒ `fingerprint-userdata --compare` 如实报"主库/WAL/会话日志都变了"，变的原因就是复核本身；只读探针打开的都是**副本库** | 复跑 `npm run verify` / `npm run audit` / `node tools/audit/check-gaplist.mjs` 即可复核 |
| **第 151 轮收官核对（装机版 1.16.149）** | 全量 **398 文件 / 6241 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**（按文件地板 307 全过、knip 棘轮 94/59/10、门面交叉 21/21、覆盖率 54.56/46.68/49.52/57.14）；`npm run audit` **17 道 exit 0**（覆盖率 / knip / jscpd / 可达性 / 上报点分诊 / fail-open / 语法门禁 / probe-guard 自证 / 缺口清单…）。**未关闭缺口只剩 O-1**（渲染进程崩溃根因，事件驱动）。面板走查 9 项里 **8 项已在装机版量到读数（全部干净：无名图标按钮 0、间距例外不成立 0、控制台 error/exception 0）**，第 9 项 CI-CD 的显示条件精确到代码行（`PanelSidebar:199` 的 `cicdEnabled` 取决于是否禁用 `@codem/ui-misc`，本机为用户配置禁用）——如实定性为配置条件，不是缺陷。**如实标注**：这些证明「这一版代码在门禁与全量用例下自洽、装机版执行的就是这份产物（sha256 逐字节一致）」，不等于「产品在任何使用路径下都没有缺陷」 | 复跑 `npm run verify` / `npm run audit` / `node tools/audit/check-gaplist.mjs` 即可复核 |
| **第 128 轮收口核对（装机版 1.16.149）** | 全量 **398 文件 / 6241 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**（地板 307 全过、knip 棘轮 94/59/10、门面交叉 21/21）；`npm run audit` **17 道 exit 0**；未关闭缺口**只剩 O-1**。**如实标注**：这只证明「这一版代码在门禁与全量用例下自洽」，不等于「产品没有任何缺陷」——最近几轮的发现集中在**审计仪器自己**（同一个坑踩了 7 次），产品侧真机读数已连续多轮干净 | 复跑这几条命令即可复核；`node tools/audit/check-gaplist.mjs` 打印当前未关闭清单 |
| **走查的「只读」还没做到（1.16.149，第 122 轮，指纹抓到）** | 第 121 轮发现走查会写用户库之后，本轮给走查配了结果侧判据 `tools/audit/fingerprint-userdata.mjs`（主库+WAL+shm 的 sha256）。**两次重跑，指纹两次都红**（WAL sha256 变化）。第一次红的原因很具体：第 117 轮接线**只接在阶段 A**，而「运行登录测试」在**阶段 B（设置页签）**⇒ 它根本没经过护栏；已把 B/C 也接上（第二次重跑可见阶段 C 的「重置对位」被 destructive 拒）。**但第二次仍然红** ⇒ 还有别的入口在写，而指纹只说「写了」、不说「哪一条写的」。**所以这一项没有关闭**，下一步：把指纹下沉到 `walkOne()` 里逐条记录「这一条有没有写库」，再从 148 条里点出是哪几条；在拿到那张清单前**不声称走查是只读的** | 读数 `ui-walk-r122.json` / `ui-walk-r122b.json`；快照 `_fp-r122-before.json` / `_fp-r122b-before.json`。**下一步**：逐条指纹（walkOne 前后各取一次）→ 得到「哪几条会写库」的清单 → 逐条决定进名单还是只记录 **第 123 轮：已下沉到逐条指纹**（`walkOne()` 前后各取主库+WAL 的 sha256，写进读数 `dbWrite`）—— 实测 **142 条里 140 条只读、2 条会写**：设置里的「API 模式…」与「CLI 模式…」两个模式页签（点它会持久化所选模式：只有 WAL 变、主库哈希不变、控制台 0/0/0，没碰内容数据）。护栏现在覆盖 A/B/C 三阶段，跳过数 11 → 32。**如实标注**：这两条具体写了哪张表/哪个键没查（要读审计行或 diff 表计数） |
| **逐面板走查（装机版 1.16.149，第 118 轮）** | 用走查脚本（`.preview-shot/ui-walk-r72.mjs`，这次**排除名单已换成仓库里的护栏** `tools/audit/probe-guard.mjs`）跑了 **148 个入口**：**控制台 error/exception 0**、**无名图标按钮 0**、小命中区里**间距例外不成立 0**、入口没找到 0、复位失败 0、点击被遮挡 0；跳过 **11** 个 —— **全部是护栏判的**且分类正确：标题栏「最小化/最大化/关闭」→ `window-control`；「切换执行模式/切换主题/切换终端」+「新对话」×3 → `global-state`；「删除该会话」×2 → `destructive`（第 94 轮那种「真的点了关闭 = 关掉用户窗口」在这轮不可能发生）。**唯一非 0 的「真控件被真控件挡住」有 4 个入口**（`coveredBy.realControl` 1~2，被挡的是插件场景里的 `.lo-pixel-room`，挡住的是 `DIV.titlebar-drag-region`）——**如实标注**：这一格**没拿到干净复核**。两次尝试都失败在仪器上：①只看中心点 ⇒ 把「中心在视口外」与「被我自己刚开的模态盖住」都算成缺陷（报出 9 个，是假数据）；②改成「房间可见区域里有没有可点位置」的 5×5 网格后，仍因**导航没到位**（`子智能体` 页签没找到、误开了菜单）而污染 ⇒ 所以**既不声称「没问题」、也不声称「有 8 个缺陷」**。干净复核的条件已写明：用走查脚本自己的开面板代码导航 + 开测前先复位浮层 + 要求房间大部分在视口内 | `node .preview-shot/ui-walk-r72.mjs .preview-shot/ui-walk-r118.json`（原始读数）、`node .preview-shot/_walk-summarize.mjs <json>`（汇总：只挑控制台错误/无名按钮/间距例外/走查自身可信度/护栏拒绝五类） |
| **首轮工具表不再缺件（O-25）的装机版复核**（第 116 轮，1.16.149） | ①**更新器自己把装机版从 1.16.148 升到 1.16.149**：按钮文案序列 `检查更新` → `检查中…` → `发现新版本 1.16.149，下载中…`，`%LOCALAPPDATA%\Codem\codem.exe` 的 ProductVersion 实测 **1.16.149**（安装器重启应用后仍在运行）。②**跑的就是含修复的那份产物**：在运行中的装机版里取回它当前加载的 **27 个 JS 资源**算 sha256 —— 含 `whenToolsReady` 的正是 `main-BGrSViAf.js`，sha256 `38bef4d2f73c5a8073adc308b60743f089d1f44e49f35e62974ad0309ddee4f7` 与本地构建产物**逐字节一致**；启动后控制台 error/exception **0**。③⚠️ **如实标注**：这证明「装机版执行的就是含修复的构建产物」；「首轮工具表一定齐全」由 4 条契约用例（`llm-tools-ready.test.ts`）守着 —— 真机上没法制造「第一条消息早于动态 import 完成」的时序 | `.preview-shot/verify-toolsready-shipped.mjs`（sha256 逐字节对齐，比字符串标志物硬）、`.preview-shot/_observe-update-flow.mjs`（更新器文案序列） |
| GitHub Latest | v1.16.149 | `node tools/release/verify-update-manifest.mjs --remote` **7/7** |
| 仓库 `latest.json` | 1.16.149 | 同上 |
| 本轮版本 | 1.16.149 | — |
| **宠物窗的崩溃兜底接上了**（第 108 轮修的缺陷，装机版实测） | `PetErrorBoundary`（第 44 轮专为宠物窗写的自包含边界）**从来没接线** —— 入口一直直接渲染 `<PetWindowApp />`，于是宠物窗渲染出错就是一块**透明死窗口**（无文字、无按钮、主窗控制台也看不到）。现在接线：崩溃显示诊断文本 + 「重新加载宠物界面」，连点 3 次给「可右键窗口退出」。**真机读数**：在运行中的 1.16.148 里 `fetch` 宠物窗自己的产物 `assets/pet-D0nvLwKC.js` ⇒ 三处标志物（`宠物窗口渲染出错` / `重新加载宠物界面` / `data-pet-error`）全在，主窗控制台 error 0 / warning 0 / exception 0。⚠️ **如实标注**：这证明"接线后的代码在装机包里"；"崩溃时窗口里真显示那行字"由 4 条用例 + **变异 6/6** 守着（真机上没法在不改坏宠物窗的前提下制造渲染崩溃） | `.preview-shot/verify-pet-boundary-shipped.mjs`（从 dist 找承载文件 → 在装机版里 fetch 它）、`src/test/pet-error-boundary-wired.test.tsx`、`.preview-shot/mutate-pet-error-boundary.mjs` |

| **确认框真的会弹**（第 84 轮修的根因，装机版实测） | 在 1.16.131 点需要确认的入口（标题栏「切换执行模式」）：控制台 **error 0**（改前必现 `Command plugin:dialog|confirm not allowed by ACL`）；枚举顶层窗口看到 **Win32 对话框窗口**（类名 `#32770`、标题 Codem）出现，且**主窗口被 disable**（模态的典型形态）；向该窗口 PostMessage 一个回车后它消失、主窗口恢复 enabled | `.preview-shot/verify-confirm-dialog-real.mjs`（点击 + 控制台增量）、`.preview-shot/_list-windows.mjs`（窗口枚举）、`.preview-shot/_dismiss-native-dialog.mjs`（P/Invoke 关框） |
| **皮肤/头像可访问性**（第 83 轮修的，装机版复核） | 皮肤卡片 **3/3 是 `<button type=button>`、3/3 可聚焦、3/3 带 `aria-pressed`、恰好 1 个选中、名字互不相同**（皮肤：默认/Hub/梦幻）、网格 `role=group`；头像预设 **50/50 各有互不相同的 `aria-label`（预设头像 1…50）、50/50 带 `aria-pressed`**、装饰图 `alt` 为空；用修好后的度量重数：**interactives 50 / unnamed 0** | `.preview-shot/verify-a11y-skin-avatar.mjs` |
| **更新下载重试**（第 82 轮修的缺陷） | 装机版点「检查更新」→ `检查中…` → `未发现更新（当前 v1.16.127）—— 更新清单已读到，里面没有更高的版本；若刚发布，CDN 可能还没同步`；控制台 error **0**（1 条 warning 是"未安装更新：none"的正常留痕）。**重试路径的真机端到端**要等下一次有可升级版本（见 O-13） | `.preview-shot/_observe-update-flow.mjs`（逐次记录按钮文案变化 + 控制台增量） |
| **修复确实进了装机包** | `dist/assets/main-*.js` 含 `下载中断，正在重试` 与 `update-retry`，且**安装包构建时间（14:37:38）晚于 dist（14:34:48）** ⇒ 打进包的是含修复的前端产物 | 时间戳判据 + `Select-String dist\assets\main-*.js` |
| **沙箱开关 fail-open 修复确实在装机版里**（第 87 轮修的缺陷） | 装机版 **1.16.134**（版本号来自 `%LOCALAPPDATA%\Codem\codem.exe` 的 ProductVersion，由更新器自己从 1.16.133 升上来）。在这份**正在运行**的应用里取回它实际加载的 `assets/main-*.js`，四项标志物全在：`读取沙箱设置失败，已沿用上次成功读到的值`、`界面上的开关不代表本次实际生效状态`、`sandbox.readSetting`、`codem-sandbox-enabled`；启动后控制台 **error 0 / warning 0 / exception 0**。⚠️ 标志物只能证明**这段代码打进去了**；「读失败时沿用上次值」这个**行为**由门禁 `sandbox-fail-open.test.ts`（SBX-1…4）+ 扫描器变异自证守着（真机上无法制造一次「设置读取失败」） | `.preview-shot/verify-sandbox-fix-shipped.mjs`（用字符串字面量当标志物 —— 生产构建会重命名标识符，用局部变量名会永远找不到） |
| **「发现 ≠ 失败」在装机版上实测**（第 88 轮修的缺陷） | 隔离钻取（副本库 + `CODEM_DB_PATH`）跑在装机版 **1.16.135** 上：控制台 **error 0**，自检发现走 `[Advisory] maintenance.invariantAudit.new：…`（改前是 `[PersistFailure] … 写盘失败（第 1 次）… —— 本次改动只存在于内存，重启后可能丢失。`）；界面上那条提示条的类名是 **`persist-alert is-advisory`**，文案为「存储自检：本次新发现记录与界面不一致：…。自检跑成了（这是它报出的结果）；这些缺口**不影响本次使用**…」—— **不再出现**「该功能本次不可用，请重试或检查日志」（改前那句是假建议：再跑一次还是同样的发现）。同时**凭据普查不再误报**（`dsh1:` + base64 的密文现在算密文，改前被说成「明文凭据」并弹出横幅） | `.preview-shot/drill-sealed-unreadable.mjs`（准备/启动/还原）+ `.preview-shot/_drill-observe.mjs`（读提示条与 `.persist-alert` 类名） |
| **钻取没有写进真库**（越界自证） | 只看 sha256 不够：正常使用（WAL 折叠/维护）本来就会改主库。所以用**决定性判据**：钻取往副本里写的那段假密文（`dsh1:`+base64 的指纹）在真库主库 / WAL / SHM 里**一次都不出现**，而同一指纹在副本库里**在**（阳性对照）。真库里 `dsh1:` 出现 3 次（主库）+ 55 次（WAL）= 真凭据仍在 | `node .preview-shot/audit-drill-touched-real-db.mjs`（只读文件字节，不经引擎打开，避免改变 WAL） |
| **更新器重试的真机端到端**（O-13 的条件终于出现，装机版实测） | 1.16.137 → 1.16.138 的升级**真的被网络掐断了三次**，于是 1.16.127 那版修复的两半**都在真机上被看到了**：① 有界重试 —— 按钮文案序列 `发现新版本 1.16.138，下载中…` → `下载中断，正在重试（第 2/3 次，等了 1s）…` → `下载中断，正在重试（第 3/3 次，等了 2s）…`，控制台 `[updater] 下载第 1/3 次失败（可重试）：error decoding response body`（两次）；② **可读失败**（原因 + 下一步 + 保留原文）—— `更新失败：网络把安装包下载掐断了（传输未完成）—— 请重试；若反复失败，可到 GitHub Release 手动下载（原始信息：error decoding response body）`。随后再点一次「检查更新」一次成功，装机版升到 1.16.138 | `.preview-shot/_observe-update-flow.mjs`（逐次记录按钮文案变化 + 控制台增量，**警告边跑边打**） |
| **凭据「解不开」的提示条**（第 89 轮修的缺口，装机版实测） | 同一钻取跑在装机版 **1.16.136** 上：界面出现 `persist-alert is-action` 提示条 ——「凭据：本机解不开已保存的密钥：1 个 provider 的密钥解不开（密文已保留，未被删除）。这些 provider 现在**用不了**（界面显示已配置、请求却没有密钥）。请在这台机器上重新填写它们的 API Key；「设置 → 安全」里有同一处状态与入口。」（**改前：界面上一条提示都没有**）；控制台对应 `[PersistFailure] secrets.unseal 凭据：本机解不开已保存的密钥（第 n 次）：…`（⚠️ 这里 error **2** 条是**正确**的：这条失败被两个调用点各报一次，通道按 area 累计次数 —— 与「静默」是两回事）。**正常库**上启动：零提示、error/warning/exception **0**（「一切正常零上报」在真机上成立） | `.preview-shot/drill-sealed-unreadable.mjs` + `.preview-shot/_drill-observe.mjs`（读 `.persist-alert` 与类名）、`.preview-shot/verify-sandbox-fix-shipped.mjs`（正常库启动读数） |
| **提示条文案不再有重复句号**（第 90 轮修的缺陷，装机版实测） | 同一钻取跑在装机版 **1.16.137** 上，提示条原文末尾是「…请在这台机器上重新填写它们的 API Key；「设置 → 安全」里有同一处状态与入口**。**」—— **单个句号**（改前 1.16.136 是「入口**。。**」，真机原文）；两条提示的语气也各自正确：`persist-alert is-action`（解不开 ⇒ 失败）+ `persist-alert is-advisory`（自检发现 ⇒ 提醒）。**正常库**上启动：零提示、error/warning/exception **0** | `.preview-shot/drill-sealed-unreadable.mjs` + `.preview-shot/_drill-observe.mjs`（读 `.persist-alert` 的类名与文本） |
| **图标按钮的真机复核**（第 91 轮修的缺口，装机版实测） | 装机版 **1.16.138** 上**在运行时**量（不只看源码）：遍历主界面 + 8 个面板，收集所有可见 `<button>`，按「名字为空（aria-label / aria-labelledby / title / 后代 img[alt] / textContent 全空）且内容是图标」判定 ⇒ **9 个视图累计 0 处**；静态扫描（`node .preview-shot/audit-icon-buttons.mjs`）同样 **0 处**。⚠️ 本轮同时**更正了扫描器第四类误报**：原来报 51 处，其中 **39 处是误报**（「文字来自变量」的表达式、嵌套花括号里的文字没被扫到）—— 那 39 处**本来就有可见文字**，加 `aria-label` 会盖掉读屏要念的话，所以一律没动 | `.preview-shot/verify-icon-a11y-live.mjs`（CDP 运行时度量）、`.preview-shot/audit-icon-buttons.mjs`（静态清单） |
| **附件正文的端到端**（第 92 轮两个缺陷的最终复核，装机版实测） | 装机版 **1.16.140** 上跑完整闭环（挂附件 → 重启 → 点开）：① 附件条**看得见**（`content-check.txt 44 B`，`.message-attachments` / `.attachment-name` / `.attachment-size`）；② **点开后 1 秒内正文出现**（`[attachment-preview] ATTACH-CONTENT-FP-4b7e18 …`，指纹与库里一致）；③ 控制台先一行 `[getAttachmentContent] … 尚未预热，已触发预取`，随后正文到位 —— 正是设计的那条路（同步缓存未命中 → 触发预取 → 有界重试命中）。改前同一操作：正文**永远不出现**，因为 `content` 已被读改写抹成 NULL（C-27） | `.preview-shot/verify-attachment-content-live.mjs`（一键跑完挂载/重启/点击/采样）、`repro-attachment-content-wipe.mjs`（引擎级复现） |
| **O-12 ②③ 的真机读数**（第 93 轮补走查，装机版 1.16.140） | ② **项目管理器**（入口=侧栏「项目」小节的 **+**，`Sidebar.tsx:635`）：以 role=dialog / aria-modal 打开，标题「项目管理」，列出 1 个项目（mimo-gui / `C:\mimo-gui` / 导入的项目），按钮命中区逐个量过（关闭 24×34、四个动作 255×52、环境配置 365×27、删除项目 32×27），控制台 **error 0**，「关闭」可用且**确实关掉了**；③ **知识图谱**（知识笔记本 → 卡片 → 工作区 → 图谱页签）：DOM 里 **33 节点 / 49 条边**，与库里 `graph_nodes=33` / `graph_edges=49` **一致**；图谱自己的提示写着「单击选中 · 双击打开文档 · 右键菜单」——**单击**只选中（`selected`），**双击**节点 `ISA-95` 打开了来源原文视图（`nb-source-viewer-overlay`），视图内容里**含该节点的 label**（原文正是「以 ISA-95 / ISO 62264（IEC 62264）标准定义的制造运行管理（MOM）为基础…」）⇒ 界面的图与库里的节点/边、以及节点到来源的对应**三面对得上**，控制台 **error 0 / warning 0** | `node .preview-shot/walk-o12-panels-v4.mjs`（②③ 一起走）、`walk-o12-graph-dblclick.mjs`（双击跨面对照）、`close-notebook-overlay.mjs`（复位浮层） |
| **命中区修复的装机版复核**（第 94 轮，1.16.141） | 同一份走查（157 个入口 / **140 个面板读数**）在装机版 1.16.141 上重跑：**收件箱面板 `small 7 → 0`**（`realSmall 0`、`padded 1`）、控制台有错的条目 **0**；覆盖分类 `overlayArtifact 1483 / sticky 194 / decorative 0 / realControl 2`（那 2 处是插件装饰方块被菜单栏压住，不构成用户可见问题） | `node .preview-shot/ui-walk-r72.mjs .preview-shot/ui-walk-r94c.json`（度量含覆盖分类） |
| **第 95 轮复核：命中区与命名**（装机版 1.16.142） | 同一份走查（**140 个面板读数 / 控制台有错 0**）重跑：`.api-key-toggle`（36×19）、`.session-recovery-close` / `.usage-stats-close`（22×22）**都已不在「小于 24」的清单里**。剩下的「小元素」逐个按 **WCAG 2.5.8 的间距例外**量了中心距（新增 `spacingOk`/`nearestDist`）：`INPUT 13×13`（×7）最近目标 **42px**、`INPUT.icon-md 16×16`（×4）**114px**、语音 `INPUT 160×16`（×3）**62px**、`INPUT 548×16`（×2）**67px**、宠物滑杆 `558×16` **171px**、`560×14` **334px**、`git-env-input 548×18` **27px**、内联链接 `A 82×12` **155px**、插件装饰方块 `27×23` **45px** ⇒ **间距例外成立**；**只剩 1 处不成立**：设置→外观的 `INPUT.sp-flex-fill`（502×16，最近目标仅 **6px**） | `node .preview-shot/ui-walk-r72.mjs .preview-shot/ui-walk-r95.json`（度量含 `spacingOk`/`nearestDist`）、`node tools/ui-audit/scan-labeled-inputs.mjs`（命名，报 0 处） |
| **第 96 轮复核：小元素全部合规**（装机版 1.16.143） | 同一份走查（**140 个面板读数 / 控制台有错 0**）：外观页那个滑块（`INPUT.sp-flex-fill` 502×16、与最近目标仅隔 6px）**已不在「小于 24」清单里**；剩下的「小元素」逐个按 WCAG 2.5.8 的间距例外量中心距 —— `INPUT 13×13`（×7）**42px**、`INPUT.icon-md 16×16`（×4）**114px**、语音 `160×16`（×3）**66px**、`548×16`（×2）**67px**、`560×14` **334px**、`git-env-input 548×18` **29px**、内联链接 `A 82×12` **155px**、插件装饰方块 `27×23` **46px** ⇒ **`spacingOk=False` 的条目为 0** | `node .preview-shot/ui-walk-r72.mjs .preview-shot/ui-walk-r96.json`（度量含 `spacingOk`/`nearestDist`） |
| **门禁全绿（第 96 轮实测）** | `npm run verify` 退出码 **0**（覆盖率棘轮 + **按文件地板 309 个文件** + knip 棘轮 + jscpd），**386 文件 / 6133 通过 / 16 跳过 / 0 失败**；`npm run audit` **12 道 exit 0** | 直接跑这两条命令看退出码 |
| **门禁全绿（第 97 轮实测，本轮不出包）** | 全量 **389 文件 / 6162 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**（按文件地板 **309 个文件**全在地板之上；jscpd **170 clones**，上一轮 171）；`npm run audit` **12 道 exit 0**。装机版仍是 **1.16.143** —— 本轮无用户可见变更（判据见 CHANGELOG「[未发布] 第 97 轮」开头：`dist/` 里没有任何技能脚本字符串、`tauri.conf.json` 无 `resources`） | 同上；覆盖数字来自 `node .preview-shot/_coverage-inventory.mjs` |
| **门禁全绿（第 98 轮实测，装机版 1.16.144）** | 全量 **391 文件 / 6183 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**（按文件地板 **309 个文件**全在地板之上；jscpd **170 clones**）；`npm run audit` **13 道 exit 0**（第 13 道是本轮新增的正则 Unicode 门禁）。**更新器自己从 1.16.143 升到 1.16.144**，途中又遇到一次网络掐断并**如实走了重试**：`[updater] 下载第 1/3 次失败（可重试）：error decoding response body` → `下载中断，正在重试（第 2/3 次，等了 1s）…` → 升级成功 | 直接跑这几条命令看退出码；版本号读 `%LOCALAPPDATA%\Codem\codem.exe` 的 ProductVersion |
| **导入修复确实在装机版里**（第 98 轮修的缺陷，装机版实测） | 在**正在运行的 1.16.144** 里通过 CDP 把它当前加载的 JS 取回来找标志物：修复后的正则 `(?:📝\|📊)`（Vite 生产构建**保留原样 emoji**，不是 `\uD83D` 转义 —— 第一版按转义形态找，扫错了地方）在 `assets/main-xpF50IaN.js` 里**在**，老的无 `u` 字符类形态 `[📝📊]` **不在**；`知识笔记本` 面板能开（`导入 Markdown` 按钮在、1 张卡片）、**控制台 error 0 / warning 0 / exception 0**。⚠️ **如实标注**：标志物只证明**这段代码打进去了**；「导入之后标题与正文长什么样」这一格**没有真机读数**（导入要过 Windows 原生文件选择框，`SendKeys` / 裸 `PostMessage(WM_CHAR)` / UI Automation / 真实鼠标输入四套路子都试不通，逐条记在脚本头部）—— 它由 `knowledge-export-import.test.ts` **17 条往返门禁 + 9/9 变异**守着 | `.preview-shot/verify-import-fix-shipped.mjs`（打开面板 + 取回运行中资源找标志物 + 控制台增量）；自动化失败的复现记录在 `.preview-shot/drill-notebook-import.mjs` |
| **`.pptx` 走查这一格量到了**（O-12 ① 关闭，第 99 轮，装机版 1.16.145） | 同一钻取在**修复前后的装机版**上各跑一遍（只动副本数据根）：入口是 `<input type="file" class="ppt-studio-hidden-input">` + CDP `DOM.setFileInputFiles`（**绕开**了自动化不了的原生对话框）。**1.16.144（修复前）**：缩略图 **5** / 画布 **1280×720** / 每页 **元素 0**、正文一个字都搜不到；**1.16.145（修复后）**逐页读数：第1页 元素2+标题、第2页 元素2+**「第一条要点」「第三条要点」**、第3页 元素1（表格文字仍读不到）、第4页 元素2+**1 张 data:image 真渲染出来**、第5页 元素1（备注仍不导入）；控制台 error 0。夹具 `src/test/fixtures/pptx/third-party-deck.pptx` 由 **python-pptx 1.0.2** 生成（第三方写入器、未改写），单元门禁 9 条 + 变异 **7/7**，逐页读数与单元断言**逐项对得上** | `node .preview-shot/drill-pptx-import.mjs`（含副本越界自证：真库主库 sha256 一致、夹具指纹真库 0 命中）、`.preview-shot/_probe-ppt-entry.mjs`（入口是 `.nb-studio-btn`→`.nb-studio-ppt`，不是猜的） |
| **门禁全绿（第 99 轮实测，装机版 1.16.145）** | 全量 **392 文件 / 6192 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**（按文件地板 **309 个文件**全在地板之上；jscpd **169 clones**；**全局 lines 阈值按棘轮从 52 升到 55** —— 本轮实测 56.02%，`coverage-baseline --check` 抓出来的）；`npm run audit` **13 道 exit 0**；零覆盖文件 **4 个 / 553 行** | 直接跑这几条命令看退出码 |
| **门禁全绿（第 102 轮实测，无出包）** | 全量 **392 文件 / 6192 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**（阈值对账 ✅「都在实测之下、棘轮之内」；按文件地板 **307 个文件**，另清理了 1 个指向已删文件的僵尸条目；jscpd **167 clones**，上一轮 169；knip 棘轮已按提示 `--update` 收紧到 **exports 342 / types 218**）；`npm run audit` **13 道 exit 0**；零覆盖文件 **2 个 / 379 行** | 直接跑这几条命令看退出码；knip 收紧用的是 `node tools/audit/knip-gate.mjs --update` |
| **上报点分诊清零**（第 104 轮，装机版 1.16.147） | **217 处上报点：已分诊 217 / 待分诊 0**（第 90 轮起点 94/123 → 第 100 轮 131/86 → **本轮 217/0**）。本批 86 处覆盖 48 个文件，逐处写了「为什么不是另一种通道」；其中 **3 处改了通道**：`todo.updateStatus` 的「列表不存在」「todos 是坏 JSON」→ action（勾选没生效、什么都没写），`eventProjection.compaction` → **advisory**（投影**没有失败**：它降级处理并继续跑完，属发现）。棘轮基线上限同步 **86 → 0**：**新上报点必须当场分诊才能合入**。真机：在**运行中的装机版**里直接取回承载新文案的分包（`event-projection-*.js` / `session-log-bridge-*.js`），新文案都在、旧假话`已回退旧路径重试`搜不到，控制台 error 0 / warning 0 / exception 0。**如实标注**：这几条的触发条件真机造不出来，语气由闸门 + 分诊理由守着 | `node tools/audit/scan-report-sites.mjs --check`（未分诊 0、漂移 0、过期 0）、`.preview-shot/_triage-report-sites-r104.mjs`、`.preview-shot/verify-report-wording-shipped-1147.mjs`（**从 dist 找承载文件 → 在装机版里 fetch 那份文件**核对） |
| **门禁全绿（第 104 轮实测，装机版 1.16.147）** | 全量 **394 文件 / 6215 通过 / 16 跳过 / 0 失败**；`tsc` **0**；`npm run verify` 退出码 **0**（阈值对账 ✅、按文件地板 307 个文件、knip 无增长、jscpd 167 clones）；`npm run audit` **13 道 exit 0**；零覆盖文件 0 个（O-21 已关） | 直接跑这几条命令看退出码 |
| **上报语气修复确实在装机版里**（第 100 轮修的缺陷，装机版实测） | 在**运行中的 1.16.146** 里取回它当前加载的 JS（先开一次知识笔记本面板，确保按需读那条路径所在的分包真的被取下来）：新文案 `知识库文本块超出缓存预算` 与建议句 `这是容量上限而不是读取失败` 都在 `assets/main-XuAI1Tla.js` 里；控制台 error 0 / warning 0 / exception 0。⚠️ **如实标注**：这 8 处提示的**触发条件在真机上造不出来**（要"读失败 / 端口被换 / 超出缓存预算"），所以「语气对不对」是由上报点闸门（通道漂移）+ **8/8 变异**守着的，不是在装机版上点出来的 | `.preview-shot/verify-report-wording-shipped.mjs`（打开面板 + 取回运行中资源找标志物 + 控制台增量） |
| **上报点分诊进度（第 100 轮）** | **217 处上报点：已分诊 131 / 待分诊 86**（第 90 轮起点是 94/123）。本批 37 处覆盖 inbox(8)/squad(8)/flashcard(6)/issue(5)/knowledge-storage(6)/show-todo(2)/file-change-tracker(2)，**其中 8 处改了通道**（读侧事件从 persist 挪到 action/advisory）。棘轮基线同步写进 `report-site-classification.test.ts` 的 `PENDING_BASELINE`（123 → **86**） | `node tools/audit/scan-report-sites.mjs --check`（未分诊 0、漂移 0、过期 0）、`.preview-shot/_triage-report-sites-r100.mjs`（干跑 / `--apply`）、变异 **8/8** |
| **看板拖拽的真机读数**（第 101 轮，装机版 1.16.146） | 走查清单里「看板拖拽」原来只能写"试不了"（它是 **HTML5 DnD**，普通鼠标事件序列不触发）。补上 `Input.setInterceptDrags` + `Input.dispatchDragEvent`（Puppeteer 的做法）后量到：卡片刻度 **待办 → Backlog**，库里 `issues.status` **todo → backlog**（**跨面对照一致**），系统评论 **0 → 1**；**拖回同一列**时评论数 **1 → 1**、状态不变（代码里"拖回原列不算变更"的守卫成立）；控制台 **error 0 / exception 0**，7 列标签与顺序与 `issue-status-meta.ts` 完全一致。钻取只写副本库：真库主库 sha256 一致、钻取指纹真库 **0 命中**（副本阳性对照 **3 / 9** 命中） | `node .preview-shot/drill-board-drag.mjs`（种子议题写副本库、结构性列识别、越界自证都在脚本里） |
| **knip 棘轮本轮抓到一次回归（已修）** | 第 89 轮新增模块时把 5 个只在内部使用的类型写成了 `export interface` ⇒ 棘轮如实报 **types 222 → 225**（`npm run verify` 因此退出码 1）。处置：改回模块内私有（**纯类型改动、零行为变化**），棘轮回到 **222/222**、verify 退出码 0。这是棘轮按设计工作的记录：**「未使用的导出」涨了就会红** | `node tools/audit/knip-gate.mjs`（输出里直接给「涨了：222 → 225」与样例文件） |
| **上下文面板口径自洽**（第 81 轮修的缺陷，装机版复核） | 同一会话同一组数字：改前 `21% ⇒ 压力等级 临界 + 🔴 即将满`；改后 **`21% ⇒ 压力等级 正常`、无告警条** | `node .preview-shot/verify-context-pair.mjs`（读的是**界面上真实渲染的文字**，并按 0.5/0.7/0.9 反推应有等级） |
| 迁移演练（在**副本**上跑） | 16 表 / 3990 行导入，逐表摘要全对；`--verify` 全部一致（新库 3991 行）；旧库 sha256 未变 | `node tools/migrate/storage-migrate.mjs --apply --src <副本> --dst <副本>` 然后 `--verify` |
| **冷启动 / 稳态基线刷新（装机版 1.16.149，第 120 轮）** | 工具 `.preview-shot/_coldstart-r120.mjs`（kill → 带 CDP 拉起 → 轮询到界面有内容 → 静置后读数）。**冷启动**：进程拉起 → CDP 目标出现 **313–343ms**；→ 界面真的有内容 **917–997ms**（5 次）；页面自报 `domContentLoaded` **328ms**。**稳态（静置 25 秒）**：JS 堆 **49MB ×3 次**（49/49/49）、DOM 节点 **628**、遗留浮层 **0**、提示条 **0**、控制台 error / warning / exception **0 / 0 / 0**。**产物体积**：主包 `main-BGrSViAf.js` **4.75MB**；全部 JS **21.92MB / 433 个文件**。**与上次基线（1.16.125）比**：JS 堆 **42 → 49MB**（+17%）、DOM 节点 **617 → 628**、控制台 error **0 → 0**。⚠️ **方法教训（本轮自己踩的）**：第一次读数报 **109MB**，如果照单全收就会写成「2.6 倍内存回归」；连跑 5 次才看清它是**离群值**（那次之前刚做过构建+反复杀进程）—— 所以**单次堆读数不算基线**，要按「N≥3 次 + 报中位数 + 标出离群」来量。这条已写进下面的下一步 | `node .preview-shot/_coldstart-r120.mjs [静置秒数]`（会重启窗口；结束不自动重启，由调用方决定）。**下一步**：把这套读数接成**常驻门禁**时，先解决「单次读数不算基线」这件事（N≥3 中位数 + 必要时强制 GC），否则门禁会像第 109–114 轮的覆盖率地板那样偶发假红 **第 124 轮：口径落地成工具** `tools/bench/coldstart-median.mjs`（N 次 + 中位数 + 离散度 + 基线阈值：JS 堆 1.5×、拉起→界面 2×、控制台 error/exception 必须 0；`--write-baseline` 记录）。实测 3 轮：拉起→目标 **455ms**、→界面 **942ms**（939–960）、JS 堆中位数 **73MB**（**离散 49–73**）、DOM **628**、控制台 **0/0/0**。**如实标注**：这个指标的抖动（1.5×）比阈值本身还大 ⇒ 现在的 1.5× 阈值只是「别把噪声当回归」的护栏；要做成真正的内存上界门禁，得先 `--runs 5`+ 或读前强制 GC（`HeapProfiler.collectGarbage`） **第 126 轮：把仪器做稳了** —— 读数前通过 CDP 强制一次 GC（`HeapProfiler.collectGarbage`，默认开、可用 `--no-gc` 对比）。同一构建 3 轮：不 GC 是 49/73/73（中位数 73、离散 1.5 倍），**强制 GC 后是 41/41/41（中位数 41、离散 0）** ⇒ 第 124 轮记进基线的那 73MB **量的是垃圾不是占用**（也解释了第 120 轮那三个数 41/49/109）。本轮中位数：拉起→目标 483ms、→界面 930ms（751–963）、DOM 628、控制台 0/0/0。**如实标注**：`coldstart-baseline.json` 里仍是第 124 轮不 GC 的中位数（73），下次 `--write-baseline` 会按新口径覆盖 |
| 冷启动读数（装机版 1.16.125） | JS 堆 **42MB** / DOM **617** 节点 / 25 秒内控制台 error 0、exception 0（1 条 `[Engine] CLI mode: no account found` 属预期） | `.preview-shot/_verify-1125.mjs` |
| **长跑门禁**（第 81 轮新增，已跑通） | 8 分钟 / 19 次采样 / **114 次面板切换**：堆 41→42MB、DOM 844 恒定、句柄 362→364、进程树 1266→1282MB（24 个 msedgewebview2）—— **无越界、无单调增长、且确有负载** | `node .preview-shot/stability-longrun.mjs --minutes 8 --interval 20`（判据与样本都在脚本里） |

> 接手前先读 **`docs/HANDOFF.md`**（2026-09-25 交接单：机器/仓库状态、微信 iLink 协议要点与请求头、未关闭项的下一步、发布流程、纪律清单）。

## 二、当前**未关闭**的项

| # | 项 | 现状（事实 + 数字） | 关闭它的条件 |
| --- | --- | --- | --- |
| O-1 | **渲染进程崩溃的根因** | 用户报过一次"白屏"（窗口在、页面崩）。事后**零痕迹**：`codem-crash.log` 不存在、运行时日志无异常、Windows 事件日志无 `codem.exe` 记录、Crashpad 无 dump（被 OOM 杀掉不产生 dump）。第 71 轮交付的是**证据链 + 有界自愈**：`ProcessFailed` → 运行时日志（类型/原因/退出码/模块 + 进程树内存）、前端 20s 心跳、一次性崩溃标记 + 用户可见提示、10 分钟内最多自动重载 3 次。已用 CDP `Page.crash` 主动打死渲染进程验证过：留下 `kind=RENDER_PROCESS_EXITED(1) reason=CRASHED(3) exit_code=-2147483645`、`自动重载页面（第 1/3 次）`、下一条心跳 `uptime=0s`。**如实标注：原始那次崩溃没有复现** | 下一次真机出现时，运行时日志里出现 `ProcessFailed` 那一行（带退出码与出错模块）或崩溃前最后一跳心跳的内存水位 |
| O-2 | ~~附件读路径无法用真机数据验证~~ **已关闭（第 92 轮）** | 见下面「已关闭」里的 C-26：用副本库 + 真实权威日志 + 装机版把「附件看得见吗」量了出来 —— 元数据看得见，**但点开是空的**（正文没被取），已修并在装机版上复核 | — |
| O-3 | ~~确认框修复的"最后一公里"没端到端量过~~ **已关闭（第 84 轮）** | 见下面"已关闭"里的 C-16：真机根因找到并修好，且**在装机版上量到"框真的弹出来了"** | — |
| O-4 | **走查三类现象**（**②③ 已关闭（第 94 轮）**） | ① 无名图标按钮：**0 处**（第 91 轮清零，棘轮基线 51 → 0；**第 118 轮在装机版 1.16.149 上重跑 148 个入口，仍是 0**）。② **小命中区**：第 94 轮逐面板量了一遍 —— 收件箱面板报出 **7 处真控件 < 24×24**（每行「归档」按钮 `padding:2px` + 12px 图标 = **16×19**，而它是通知**唯一的移除入口**；「显示已归档」复选框 **13×13**，外面 label 只有 20px 高），已修（24×24 下限 + 居中）并在**装机版 1.16.141** 复核：该面板 `small 7 → 0`。**其余仍小于 24 的（没有功能阻断、属密度取向）另开 O-20**。③ **被遮挡控件**：度量升级后（按遮挡者分类）157 个入口重跑 —— `overlayArtifact` **1425**（走查自己开着模态框量后面的面板）、`sticky` **194**（面板自己的吸顶头/底栏）、`decorative` 0、`realControl` **1~2**（插件装饰方块被菜单栏压住）⇒ **原来那个「123 个面板」里没有真缺陷** | — |
| O-12 | ~~三个面板的真机走查~~ **已关闭（②③ 第 93 轮、① 第 99 轮）** | ② **项目管理器**：已走查（只读：开列表、量按钮、只按「关闭」），控制台 error 0 —— 见机器状态表「O-12 ②③ 的真机读数」。③ **知识图谱交互**：已走查并做了**跨面对照**（DOM 33 节点/49 边 = 库里 33/49；双击节点打开来源原文且含该节点 label）。① **PPT / 文档查看器**：`.docx` 早在来源视图里渲染正常；**`.pptx` 在第 99 轮补上** —— 先核实"本机没有可打开的演示文稿"这个前提（找到的三份 deck 全是空模板），再造一份 python-pptx 第三方 deck，用 CDP 喂给 `<input type=file>` 在装机版上逐页量：修复前**每页元素 0、一个字都没有**，修复后第 2 页要点、第 4 页图片都渲染出来了（见机器状态表那一行） | — |
| O-13 | ~~更新重试的「真机端到端」没跑到~~ **已关闭（第 91 轮）** | 见上面机器状态表里「更新器重试的真机端到端」那一行：1.16.137 → 1.16.138 的升级被掐断三次，重试 UI 与控制台警告、以及最终**可读的失败文案**都在真机上出现了；再点一次即成功 | — |
| O-14 | ~~探针抽屉里那 4 个语法已坏的历史脚本，改动是否真的生效~~ **已关闭（第 87 轮）** | 见下面「已关闭」里的 C-19：逐项核对**效果**（不是核对当年的代码原文） | — |
| O-5 | ~~19 处 `alert()` 没走统一入口~~ **已关闭（第 83 轮）** | 见下面"已关闭"里的 C-12：生产代码 **15 处**裸 `alert` 已全部迁到 `alertDialog`，并新增 NC-6/NC-7 两条门禁 + 2 处突变 | — |
| O-6 | ~~覆盖率只有「聚合 + 目录级」地板，没有按文件~~ **已关闭（第 96 轮）** | 见下面「已关闭」里的 C-28：按文件地板已上线（309 个文件，`npm run verify` 里跑），并且**量出了零覆盖清单**（新开 O-21 补测试） | — |
| O-7 | ~~性能/稳定性只有"当前基线"，没有回归闸门~~ **已关闭（第 81 轮）** | 见下面"已关闭"里的 C-9：长跑门禁已落地并跑通 | — |
| O-8 | ~~**knip 的未用导出/未用类型已逐条分诊（第 113 轮完成）**（473 → 153：exports 280→94、types 193→59）~~ **已关闭（第 127 轮）** （第 113 轮分诊完成；第 122 轮 21 个门面决定保留） | 已把它变成**棘轮**（`tools/audit/knip-gate.mjs` + `knip-baseline.json`：只许降不许升，在 `npm run verify` 里跑），"未使用文件"那类期望值为 **0**。**第 111 轮**：判据用"这个名字在 `src/`+`tools/`+`scripts/`+根配置里（除自己文件外）**一次都不出现**"，**落地 278 处 / 123 个文件**，实测 **473 → 195**；逐行机器比对（`+278/−278`，每行必须恰好是去掉 `export `）后才提交。**第 112 轮查清了剩下那批的病因**：AST 判出 **45 条的引用者全是模块门面 barrel**，而它们当时在 knip 的 `ignore` 里 —— `ignore` 是最钝的刀（文件整个消失，连它的 `import` 都不算数），所以被它 re-export 的符号全成了"没人用"。**实验**：只放开 ignore ⇒ exports/types 不变、**20 个 barrel 立刻变成"未使用文件"**；登记成 `entry` ⇒ **exports 113→103、types 82→68（−24）、新增 0**。已按后者改 `knip.json`。**新增交叉判据 KNIP-ENTRY-BARREL**：knip entry 的门面集合 ↔ `reachability-allowlist.json` 里以 `index.ts` 结尾的集合**必须严格相等**（实测两侧都是 21），**变异 5/5**。**顺带查明（要人做决定）**：这 **21 个模块门面当前没有任何人 import** —— 它已由可达性白名单逐条记账，不是新口径下的盲区，但"删掉 21 个死门面"是一次产品判断，未动。**第 113 轮（第三批）**：AST 分诊器**修掉自己两个坑** —— ①`export *` 再导出被当成"整个模块被引用"（一刀切拦住上百条，太钝）；②**真漏洞**：从 barrel 具名导入（形如 `import { X } from "../core/agent-teams"`）时引用记在 **barrel** 头上，而 X 的定义文件看着"没人用" ⇒ 照 v2 去 export 会**直接打断这个导入**。修法：先算 `export *` 的反向闭包，具名引用必须在**闭包里的所有面**上找；只有当某个面被"命名空间导入 / require / 动态 import"整个拿到手时，才整块拦住。**第三批落地 16 条**（含 `export { A, B } from "./x"` 这种**混合子句只摘掉无人引用的名字**，其余名字原样保留），实测 **171 → 153**（exports 103→94、types 68→59、duplicates 11→10），棘轮已收紧。**逐行自证升级为 token 级**（`.preview-shot/_verify-unexport-diff3.mjs`：去掉 `export`/`from`/`type`/标点/字符串字面量与本轮登记摘掉的名字后，**标识符+运算符序列必须逐位相同**）。**又抓到自己一个顺序 bug**：`export { … } from` 是 ExportDeclaration、**没有 export 修饰符**，我把它写在修饰符检查之后 ⇒ 一处都不触发（实测"将去掉 export：0 条"），调换顺序才生效。**153 条已全部落桶定性**：NS_BLOCKED 110（面被整个引用）/ PROD_IMPORTED 11（门面按名再导出）/ STRING_ONLY 23 / DEFAULT_EXPORT 5 / REAL_MENTION 3 / SAFE 1（声明写法特殊未动）；明细 `.preview-shot/_knip-ref-buckets3.json` | ①**逐条分诊已完成**（153 条全部落桶并写明处置理由；基线已收紧到 94/59/10）。**若还要继续降**：只能人工逐个确认 STRING_ONLY / REAL_MENTION 的"是不是同名巧合"，性价比低 —— 如实记着，不动它 ②**决定 21 个死门面的去留**：删要跑真机回归（外部插件可能按路径 import 门面），留就在文件头写明"当前无人 import" |
| O-22 | ~~已实现、有测试或已登记、但生产代码一次都没接线的模块~~ **已收口（第 110 轮）** | **工具与白名单**：`tools/audit/reachability-scan.mjs`（模块解析 + 可达性 + 两条已知答案对照）+ `tools/audit/reachability-allowlist.json`（39 个不可达文件逐条登记并写清类别），两道闸：`reachability-gate.test.ts`（REACH-1…5）+ `npm run audit:reachability`（**audit 14 道**）。**本轮把最后两处定下来："如实标注"（既不接线、也不删除）**：①`src/core/storage/sync-engine.ts`（523 行远程同步引擎：实现与用例完整、0 生产调用方、UI 无入口 —— 接上需要一次性决定后端/认证/触发时机，删掉则丢掉已测实现）；②`src/components/RegenerateModelPopover.tsx`（「重新生成」是活的，缺"选模型"的入口与下游参数 ⇒ 未接线的增强）。两个文件头现在都有**独立标记行** `@unwired 未接线：…`（写清 0 调用方、判据工具、两条路），**REACH-5** 钉住"白名单说未接线的条目必须有该标记"，**变异 6/6**。**顺带抓出判据自身的坑**：第一版用 `includes("@unwired")` 判标记，而说明文字里也提了这个词 ⇒ 删掉标记照样绿；改成"独立一行 `* @unwired …`"后才真的拦得住 | — （判据已达成：不可达集合 ⊆ 白名单、白名单无过期条目、未接线条目有代码标记）。**要接上时**：先改 `@unwired` 注释与白名单理由，再补面板走查与验收判据 |
| O-23 | **按文件覆盖率地板的「偶发假红」** **已定位并消除（第 114 轮）** | 现象（第 109 轮）：`npm run verify` 报 `src/core/agent-teams/tools.ts functions 24.13% < 地板 35%`；随后 3 次复跑都是 44.82% ⇒ 4 次里 1 次异常、未复现。**第 114 轮当场复现并定位**：复现脚本（`.preview-shot/_o23-repro.mjs`）跑 4 轮，第 1 轮 **24.13%（7/29）**、地板 🔴；第 2–4 轮 **62.06%（18/29）**、地板 ✅。**根因**：`src/core/llm/index.ts` 注册这些工具用的是**动态 import + `.then()`，不 await**（`import("../agent-teams/tools").then(...)`）——那次 import 没在测试结束前跑完，`tools.ts` 的 11 个工厂函数就**整批算未覆盖**（7/29 = 24.13%），跑完则是 18/29 = 62.06%。**不是门禁算错，是读数本身不确定。****处置**：新增 `src/test/agent-teams-tools.test.ts`（13 条，同步 import + 逐个调用 10 个工厂 + 注册顺序/shouldDefer 断言 + create→delete 跑通 + 非队长抛错），覆盖率从"看运气"变成每次都 62.06%，地板余量由 0 变成 **+27 个百分点**。**工具侧也修了自己的两个坑**（如实记）：①第一版复现脚本用 `node_modules/.bin/vitest.cmd` 起子进程，Windows 上不经 shell 起 `.cmd` **直接失败**（4 次全是 `vitest=1 / 0 秒`），而地板检查读的是**上一轮的旧 coverage-summary.json** ⇒ 那版"4 次全绿"是**假数据**，已作废；②改成 `node node_modules/vitest/vitest.mjs` 并**每轮比对 summary 的 mtime**，比开跑时间旧就判该轮无效 | —（已消除：读数不再依赖一次 fire-and-forget 的动态 import；同类"覆盖率读数不确定"可照此办法复现——先证明数据是新的，再看分布） |
| O-25 | ~~工具注册是 fire-and-forget（竞态）~~ **已关闭（第 115 轮修、第 116 轮出包并装机版复核）** | 根因：`src/core/llm/index.ts` 里 squad / issue / agent-teams / computer-use / 跨会话委派 / subagent-tools 六批工具都用 `import(spec).then(注册)` 注册，**没有任何等待点**。证据：`agent-teams/tools.ts` 的函数覆盖率在 **24.13%（7/29）** 与 **62.06%（18/29）** 之间跳（第 114 轮 4 轮实测），就是这条路径有时跑完、有时没跑完。**修法**：①新增 `LLMEngine.whenToolsReady()` —— **再 import 一次同样的说明符**并 `await Promise.allSettled([...])`；为什么这样够：ES 模块注册表对同一说明符返回同一个 promise，而我们的 `.then` 排在原注册回调**之后** ⇒ resolve 时注册一定已跑过（成败都算落定）。这样不必改六条链（每条各有 catch 分支），也不会漏掉失败路径。②`process()`（真正构建请求的 async generator）开头 `await this.whenToolsReady()`，已 resolve 时开销只是一个 microtask。**契约用例** `src/test/llm-tools-ready.test.ts` 4 条：await 后 agent-teams 十个工具在表里 / 屏障可重复 await（幂等、不重复注册）/ 两次构造互不干扰 / `process()` 入口形状不变 | —（证据见机器状态表「首轮工具表不再缺件（O-25）的装机版复核」与「已关闭」里的 C-42） |
| O-27 | ~~**微信桥：绑定成功但一条消息都收不到（轮询不取）**~~ **已关闭（第 154 轮）** | 现场（1.16.149，2026-09-25）：解绑后重扫，confirmed 成功（新 bot_id=<bot-id 已脱敏>@im.bot、session.json 14:14:23、state=connected），但**其后 91 秒内应用一次都没去取**（干净实验、不手工碰服务端）：inbound_count=0、last_inbound_at=null、last_error=null、**游标文件 91 秒零更新**。而用同一 token 与**正确请求头**（Authorization: Bearer + AuthorizationType + ilink-app-id=bot + X-WECHAT-UIN + ilink-app-clientversion + base_info.channel_version=2.4.6）手工 POST /ilink/bot/getupdates **能取到排队的两条消息**（to_user_id 正是我们绑定的 bot、type=1、含 context_token）⇒ 凭据/协议/服务端正常，**问题在客户端轮询侧**。已排除：发错 bot、入站过滤（type=1/无 group_id/文本非空）、emit_state 与 ilink_status 改 epoch（都读过）、界面频繁轮询 status（只在挂载与事件时调）。**根因未定 —— 因为这条链路零可观测性**（轮询结果/超时/未知响应形状/入站丢弃全都不计数不写日志）。相邻两处已确认缺陷：①binded_redirect 会**把旧会话当有效恢复并标 connected**（前两轮「扫了没反应」的真凶：它连的是旧 bot）；②恢复会话后不做探活，凭据失效也显示「已连接」 | ①给轮询装眼睛：每轮记 polls / last_poll_at / last_poll_error / msgs 条数并写运行时日志，ilink_status 暴露这些字段；②自愈：除 401/403 外异常一律记账退避，并加看门狗（会话有效却连续 N 轮没成功取数据 ⇒ 重建轮询）；③binded_redirect 恢复旧会话前先探活，失败即置需重新扫码；④修完出 1.16.150 并在装机版复核（判据：polls 在涨、游标开始更新、inbound_count≥1、**微信里真的收到回复**） **第 152 轮：病根找到并修掉，装机版 1.16.152 实测通过** —— 日志显示 poll#1 原始响应里**有** msgs，而应用记 0 条；对照结构体发现 `WeixinMessage.message_id` 声明为 `Option<String>`，服务端发的却是**数字** ⇒ 整批消息反序列化失败后被 `unwrap_or_default()` **静默丢掉**。已改为兼容字符串/数字 + 解析失败记账落日志。装机版实测：`inbound_count=3`（取到积压的三条「你好」）、游标开始更新、`outbound_count=3`、生成微信会话文件 ⇒ 前三条判据全过；**仅剩**「你在手机端确认收到回复」这一条，以及 `binded_redirect` 恢复旧会话前先探活那处加固（仍挂着） **第 154 轮：那处加固做掉了，O-27 关闭** —— ③`src-tauri/src/ilink/login.rs` 的 `binded_redirect` 分支恢复旧会话前先**探活**（一次 `getupdates`，新增 `PROBE_TIMEOUT = 6s`，`proto.rs`）：判据与 `poll_loop` **完全一致**（`errcode -14` / HTTP 401 / 403 ⇒ `Dead` ⇒ `expire_session` + 「旧会话已失效（…），请重新扫码」，**不再冒充已连接**；收到响应或**被服务端 hold 到本地超时** ⇒ `Alive` ⇒ 换代 + 起轮询；`is_fresh` 已过期 ⇒ 直接判失效；其它网络错 ⇒ `Unknown` ⇒ **既不判活也不判死**（"探活没做成" ≠ "会话失效"），如实写进 `last_error` 下一轮再探，8 分钟登录总超时兜底）。探活**刻意不推进游标、不分发消息** ⇒ 排队中的消息会在下一次 `getupdates` 里原样取到（不丢）。`cargo check` 通过；同一提交随 1.16.153 出包（装机版 1.16.153 启动日志实测：`[ilink] poll#1 发起 getupdates` 正常续拉，微信链路沿 1.16.152 的读数持续可用）。**如实标注**：`binded_redirect` 这条分支要真机复现得让服务端判"已绑定"（要用一张本地已过期/已解绑的 token 去扫码），本机没有第二个可解绑的微信号 ⇒ **该分支在装机版上没有被点过**；判据由代码级三态 + `cargo check` 守着，未做真机触发 |
| O-28 | ~~**微信会话里助手回复「看得见但没入事件日志」**（自检当场报出 3 条新缺口）~~ **已关闭（第 154 轮）** | 现场（装机版 1.16.152，2026-09-25，微信链路刚打通后立刻复现）：维护的不变量审计报「本次新产生 3 条缺口」，样例三条全部落在**微信会话**上：`wx-<peer-id 已脱敏>-im-wechat\|VISIBLE_BUT_NOT_RECORDED\|assistant-1790319154162`、`…-2`、`…-3`，正好对应微信桥驱动的那三次 agent 回复 ⇒ 回复**在界面上可见**，但权威日志里没有对应的 `assistant_text` 事件。同批日志里的 `703/706 条历史缺口`是迁移期遗留（水位之内），自检自己写明了「不是本次新产生的缺陷」，与这三条**不是一回事**。**如实标注**：目前只确证「微信链路会新增该类缺口」，尚未定位是哪一步漏写（微信桥经 `executeSessionTurn` 驱动回合，与界面路径同源，差别可能在回合结束后的落库/事件写入这一环） | ①用副本库复现：跑一次微信回合，比对 `messages` 与事件日志两侧，定位漏写点；②修好后补一条用例（微信驱动的回合必须同时写 `assistant_text`）；③出包并在装机版复核「新缺口 0」（自检的新增计数必须为 0）；④顺带把 `binded_redirect` 恢复旧会话前先探活那处加固一起做掉 **第 153 轮：把范围缩到一处代码** —— 双写点已找到：`src/core/session/executor.ts:508-523`（回合收尾时 `if (currentAssistantMsgId) { … getEventLog().append(sessionId, "assistant_text", { messageId, content }) }`，且 `catch` 只 `console.warn`）。所以这三条缺口只可能来自四种情况之一：①该回合结束时 `currentAssistantMsgId` 为 null（助手消息不是在 executor 这条路径里建的）；②写入抛错但被那条 `console.warn` 吞掉（用户贴的控制台里没看到这行，但那是**部分日志**）；③回合在收尾前就被中止/提前返回；④微信路径根本没有走 executor 的建消息逻辑。**下一步（判据明确）**：用副本库跑一次微信回合，打印 `currentAssistantMsgId` 与那次 `append` 的返回值（有无抛错），即可在这四种里定位；随后按定位点修 + 补用例（微信回合必须两侧同写）+ 出包复核「自检新增计数 = 0」 **第 154 轮：真因找到并修掉，装机版 1.16.153/1.16.154 实测通过 —— 上面那四种猜测全不成立，问题根本不在 `assistant_text`**。副本库（主库+WAL+SHM 复制后**只读**打开）把两侧事实摆出来：那三行 `assistant-1790319154162` / `-2` / `-3` 是**纯工具轮**的助手行（正文 len=0，按设计**不写** `assistant_text`，它的事实记在工具事件里，口径见 FWT-C1a），而它们的 `tool_call.messageId` 是 `msg-1790319153390` / `msg-1790319155791` / `msg-1790319157282` —— **引擎自造的 id**（`AgenticLoop` 的 `msg-${Date.now()+1}` 与每轮末 `msg-${…+iteration+100}`），而 `messages` 表 / `tool_calls.message_id` 用的是落库方生成的 `assistant-…`（`executor.ts` / `App.tsx`）⇒ **两套 id 从来没对上过**。危害不止自检报数：`event-projection.applyToolCall` 找不到那个 id 会**凭空建一条 `msg-…` 的助手行**，真实行在投影里反而消失。**修法**：①`AgenticLoopConfig` / `LLMEngine.process` 新增 `resolveAssistantMessageId`（助手消息 id 的**单一来源**交给落库方：executor 传 `() => ensureAssistantMessage()`、App 传 `() => assistantMsgId`，引擎拿不到才退回自造 id；`process()` 里**无条件写**含 `undefined`，否则池化复用的 loop 会把上一轮的闭包留给这一轮）；②同一处「双写点」收敛成**一个写入者**：删掉 executor 收尾时多余的那次显式 `assistant_text` append（真机实测：改前每条用户消息 / 有正文的回复都留下**两条**事件，`seq=8951/8952`、`seq=8971/8972`），只在「空正文**且**无工具调用」的收尾行补一条（否则那一行投影重建时会消失），写入失败按 `seq===0`/抛错走 `reportPersistFailure("executor.settleEmptyAssistant", …)`（新上报点已当场分诊）。**判据**：`src/test/o28-assistant-event-wiring.test.ts` **7 条**（真 `LLMEngine` + 真工具流水线 + 脚本化 provider，会话 id 就是真机出事的那个）+ **变异自证 6/6**（`.preview-shot/mutate-o28-messageid.mjs`）。**装机版复核**（这是关闭判据 ③）：1.16.153 上跑真回合 —— 主聊天一次工具回合产出的空正文纯工具轮助手行 `assistant-1790320878315`，其 `tool_call`/`tool_result` 的 `messageId` **就是行 id**（改前是 `msg-…`），同一会话 `user_message`/`assistant_text` 各 **1 条**（改前各 2 条）；再跑一次**委派**回合（正是 O-28 出事的那条 `executeSessionTurn` 路径）产出 3 条同类行，**每条的工具事件 id 都对得上**。**同一批复核又量出第三条口径差**（`system` 行永远没有事件 ⇒ 自检「本次新产生」恒非 0，样例 `err-1790320962593-3axyi`）——已在 1.16.154 收窄口径（只放宽 `system`，`user`/`assistant` 一条不动；用例 O28-6a/6b 双向钉住 + 变异 6/6 含"放宽过头"的反向变异），随后装机版复核 `[不变量水位] … 本次新产生 0 个`。**如实标注**：这一轮的复核回合走的是**主聊天 + 委派**两条路（同一套 `executeSessionTurn` 代码），**没有**再让用户从手机发微信消息；微信链路本身的端到端读数沿用 1.16.152 那次（`inbound_count=3`/`outbound_count=3`/手机端确认收到） |
| O-30 | **皮肤/UI 对标 `GCWing/OpenBitFun` 暴露出的结构性差距**（分析见 `docs/SKIN-BENCHMARK-OPENBITFUN.md`）—— **已收口**（第 165 轮；P0 四条 1.16.155、P1/P2 与暗色 D 系列 1.16.156–1.16.165，见文档 §16.4 的「门禁 → 变异自证」覆盖矩阵） | 对标分析（源码数值 + 脚本对账，非"看图"）写在 **`docs/SKIN-BENCHMARK-OPENBITFUN.md`**（取对方 `main@ded818312a39`）。**已关闭的四条 P0（1.16.155）**：①`--radius-xs` 曾被两个 `:root` 重复定义（4px vs 8px，运行时实际 8px、与 `--radius` 重复、81 处受影响）⇒ 唯一定义 4px、80 处密集控件按文档意图迁到 6px、两套皮肤补齐全套刻度，新增 `scan-token-hygiene.mjs`（H1 重复/H2 成套/H3 单调/H4 类型，audit 第 18 道）+ 用例 12 条 + **变异 5/5**；②边框补成三档（5%/9%/34%）+ 输入态（hover 20%、focus=strong）并接到输入类控件上，判据 LIGHT-UI-2c；③新增 `--lh-*`(7) / `--ls-*`(3) 令牌并把**行高 87 处、字距 7 处**换成令牌（只做精确等值替换），另加两个**有真实调用点**的排版角色（`.text-overline`/`.text-meta`）；④新增写死值棘轮 `scan-style-literals.mjs` + 基线（audit 第 19 道）+ **变异 5/5**。**顺带修掉两条暗色缺口**（对标暗色时用同一套数学量出来的）：`--text-muted` 在 `--bg-hover` 上只有 3.92:1（现 4.52，四个面全过 4.5）、暗色边框比参考实现弱一大截（1.35 → 1.54）；并补上**暗色档一直没有的不变量门禁** DARK-UI-0…5。**如实更正**：第一版对账把 `var(--x, 兜底)` 也算成"写死"（负向断言 + `\s*` 可匹配零字符），报出"颜色 540/字号 917"，**真实值是颜色裸字面量 140（主样式表只有 7 处）、字号 45** —— 已按新口径重写并在文档里留了"口径更正"一节。 | **收口记录（第 165 轮）**：对标清单逐项落地，且**每一项都有门禁 + 变异自证 + 装机版复核**——①玻璃材质（1.16.158：Rust 侧 Mica/Acrylic + `data-native-material` + 外壳透明、内容不透明、材质档关掉 CSS 模糊）；②P1-1 表面角色（1.16.162，`--surface-raised` + LIT-12：别名 / ≥4 消费方 / 不与 `--surface-1` 并存）；③P1-2 文字与品牌色解耦（1.16.159/160，文字三档由墨色派生、`--accent-*` 阶梯，LIT-10/11）；④P1-3 阴影阶梯统一 + D4 品牌紫决策（1.16.157，高度阶梯全中性、品牌色只留 `--shadow-glow`）；⑤P1-4 状态色四角色 + `--opacity-disabled`（1.16.157，LIT-8 / LIGHT-UI-12 / H5）；⑥P2-1 密度（1.16.159）+ **高对比（1.16.165）** —— 高对比此前只把玻璃关掉、没有任何令牌级覆盖，本轮补齐并量出`--text-muted` 5.28→8.75:1、`--text-secondary` 7.37→12.30:1、边框可见度 1.19→1.73:1、焦点环 2px→3px（APPEARANCE-7 + ALPHA-1…4 + A7–A11）；⑦P2-2/D8 身份色（1.16.162，IDENTITY-1/2）；⑧P2-3 皮肤数据化（1.16.163/164，hub/dream 规则体颜色字面量 0，**源码与压缩产物两个口径都是 0**，SKIN-1/2）；⑨P2-4 颜色用量审计（1.16.161，`audit:color-roles` 第 20 道 + CR-1…7）；⑩D2 内高光 / D3 暗色阴影 / D6 按下态分档 / D7 高对比均已落地。**变异自证合计 62 条**（玻璃 15 / 样式 23 / 外观 12 / 颜色角色 7 / 阴影 5），全部「注入即红 + 还原逐字节回绿」；装机版 **1.16.165** 复核通过（运行中 CSS 与 `dist` 产物 sha256 逐字节相同 ⇒ 门禁量的就是用户跑的那份）。**明确标注不做/不动手的**：①P1-1 的「结构翻转（灰工作区 + 白卡）」**不做** —— 实测「灰面占屏比」我们 32.8% / 参考实现 5.5%，整片改灰方向相反；②D1 暗色主文字亮度只记录不动手（用户说当前暗色好看）；③D5 白字压紫底 3.99 登记为 AA-large，要过 4.5 属产品决策。**仍是可选项（不构成差距）**：皮肤调色板的语义命名（`--hub-ink` 之类）与「皮肤 JSON/TS 对象 + 运行时注入」。 |
| O-29 | ~~重载/重启后「当前会话」最近的消息会被再落库一次 ⇒ 同一条正文在事件表里出现**两条**（文本事件的指纹去重表是进程内内存态）~~ **已关闭（第 176 轮）** |现场（第 154 轮做 O-28 装机版复核时顺手看到的）：在 1.16.153 上跑完一个回合后**重载页面**（启动维护会重跑），副本库里该会话出现成对重复：`seq=8991` 与 `seq=9001` 是同一条 `user_message`（`mid=user-1790320878301`，时间戳 1790320903141 = 重载那一刻）、`seq=8995` 与 `seq=9002` 是同一条 `assistant_text`（`mid=assistant-1790320880538-2`）。根因：`message.ts::appendMessageTextEvent` 的指纹表（`writtenTextEventFingerprints`）与 `event-log` 的镜像一样是**模块级内存状态**，页面一重载就空了；重载后 store 把当前会话的消息再 `saveMessages` 一次，于是同一条正文被当成"第一次写"再补一条事件。**危害如实标注**：它**不影响**"可见即已记录"（同一个 `messageId`，运行时不变量的判据仍成立，所以自检对它不报），影响的是 `session_event_search` 会搜出重复结果（第 45 轮就记过"重复行没有信息量，只会污染搜索结果"）与事件表体积 **第 176 轮收口**：①判据换成**持久判据** —— 写前回查事件日志里是否已有 (type, messageId, 内容指纹) 完全相同的事件 （内存指纹表退化为快路径；索引按会话惰性建一次并随写入增量维护；镜像未就绪时**不当作「没有事件」**而是延后判定 （whenSessionEventsLoaded 就绪或超时后重跑同一套判据），既不重复也不丢；保留 seq !== 0 才记账的规则）； ②用例 **FC-D0d**（__resetTextEventFingerprints() 模拟重载后再落库同一份正文 ⇒ 事件表不得新增行；反向对照： 正文真被改写必须补一条）；变异 O29-停用持久去重 必红（第 176 轮变异合计 33/33）； ③**真机读数（改前 → 改后）**：改前（装机版 1.16.175，只读探针只查副本库）逐会话统计 (event_type, payload.messageId, payload.content) 完全相同的行 ⇒ 合计 7951 事件 / **5853 文本事件 / 5614 条重复** （最大会话 4368 文本事件里 4319 条重复；有个会话同 4 条消息各写了 9 次 —— 本轮为逐项复核重载过多次， 即**每重载一次再写一遍**）；改成 1.16.176 后**重载 3 次**（CDP Page.reload）再数： **文本事件 5853 → 5853、重复 5614 → 5614（+0）**，逐会话明细逐项一致 ⇒ 「重载不再制造重复事件」成立。 **如实标注**：本轮做到的是「不再新增」，**历史那 5614 行还在**（另开 O-31）| 已按上述判据与读数收口；复核方式：npm run verify（FC-D0d）+ node .preview-shot/_count-dup-text-events.mjs |
| O-31 | ~~**O-29 留下的历史重复文本事件还没清理**（第 176 轮只做到「不再新增」）~~ **已关闭（第 178 轮）** | **① 先更正原条目里的三个数（它们是探针 bug，不是事实）**：原文写的「5853 条文本事件 / 5614 条重复 / 最大会话 4368/4319」全部来自 `events.list { limit: 5000 }` —— 而最大那个会话有 **5474** 条事件，于是它被**截断在 5000 行**。新探针（`.preview-shot/_count-dup-text-events2.mjs`）逐页读全并用 `events.count` **交叉校验**（两者不等直接抛，不许"读到多少算多少"）⇒ 真值 **8425 事件 / 6327 文本事件**；算术自证：**4368 + 474 = 4842**，正是该会话真实的文本事件数 —— 474 就是被切掉的那一段。**② 判据也要更正：原计划写「每组只保留最新一条（seq 最大）」是错的。** `event-projection.ts` 里 `applyUserMessage` 有 `if (state.messages.some(m => m.id === payload.messageId)) return;`（**首次胜出**），`applyAssistantText` 的位置也由**首次**那条 `push` 决定（只有正文是后写覆盖）⇒ 保留**最新**会把消息整体往后挪，是**静默的回放等价性破坏**。正确规则是保留 **`seq` 最小**。用交错流（A@10、B@20、A@30）钉住：保留最小 ⇒ 回放 `[A,B]` 与未收敛时逐条相同；保留最大 ⇒ 回放 `[B,A]`。**③ 处置（两层）**：**引擎**新增 `events.dedup_text`（`repo.rs`；**默认干跑**，`apply:true` 才动库；范围**刻意只有** `user_message`/`assistant_text` —— 工具事件靠 `toolCallId` 互相引用，删较早的 `tool_call` 会让 `tool_result` 变孤儿；判据是**载荷逐字节相同**，所以"同 messageId 被改写的正文"一条都不会删；走 `measure_delete_impact` 如实报账，删除被 `storage_audit` 的 `AFTER DELETE` 触发器留痕）；**读路**新增 `collapseExactDuplicateTextEvents`（`event-types.ts`）并接进 `session_event_search`（模型可见面从此**不可能**出现重复命中，即使维护没跑或将来又出新的重复来源）；**启动维护**里加一次性步骤（`settings` 一次性标记，成功后才写；`removed>0` 时走 advisory 告知并进汇总行）。**④ 真机读数（只查副本库，20.3MB）**：文本事件 **6327 条 / 不同载荷 258 组 ⇒ 完全重复 6069 条**；干跑 `before_rows=6327 distinct_groups=258 removable=6069` 与独立探针**逐项一致**（口径互证）；执行后 `removed=6069 → after_rows=258`，**再跑一次 removed=0**（幂等）；**回放等价性：逐会话比对投影消息共 253 条，不一致的会话 0 个**；**非文本事件 2098 → 2098（零损失）**；`audit.recent` 查得到 `session_events/DELETE` 留痕。**⑤ 口径细差如实标注**：按「解析后的 (type, messageId, content)」分组是 **6073** 条重复，按「载荷逐字节相同」是 **6069** —— 引擎用后者（**更严**），所以有 **4 行"同 mid 同正文但载荷字节不同"刻意保留**（宁可少删）。**⑥ 用例与变异**：Rust **8 条**（保留最小/内容不同不删/工具事件不动/只去重一个会话不许牵连别的会话/全库/幂等/样例与类型范围/审计留痕）+ TS **9 条**（`src/test/event-dedup.test.ts`，DEDUP-1…9，含回放等价性与"1920 条收敛到 64 条"的真实形态）；变异 **8/8 红 + 还原逐字节回绿**（含"改成保留最大"、"按 messageId 收敛"、"范围放开到全部"、"外层 DELETE 漏掉会话过滤"、"干跑会真的删"）。**⑦ 顺带修掉两只自己的虫子**：新加的返回类型写成了 `export interface` ⇒ knip 棘轮如实报 `types 59 → 60`（`verify` 退出码 1，与第 89 轮同款），改回模块内私有；变异脚本第一版有两条报"判据片段没找到" —— 那是 **CRLF vs LF** 导致变异**根本没贴上**的**假绿**，已改成两种换行都带 | 已按上述判据与读数收口；复核方式：`npm run verify`（Rust 8 条 + TS 9 条）、`node .preview-shot/mutate-o31-dedup.mjs`（8/8）、`node .preview-shot/_verify-o31-dedup.mjs`（副本上的改前/改后读数 + 回放等价性）、`node .preview-shot/_count-dup-text-events2.mjs`（重复数，**注意旧探针 `_count-dup-text-events.mjs` 有 `limit:5000` 截断，读数偏小，别再用它的汇总数**） |
| O-32 | ~~**空态的"第三族"：名字像空态、但 COND-1 的棘轮从来没数到它们的类**~~ **已关闭（第 179 轮，当场发现当场做掉）** | 发现过程：修 COND-1 的排除条件盲点（子串 `input` 把 `.input-popover-empty` 整条静默跳过）时，顺手做了一次**全仓扫描** —— TSX 里 `className` 不含 `empty-hint` 却名字像空态的元素共 **37 处**。逐条判形状后：**9 处是真空态**（`input-popover-empty` / `ppt-property-empty` / `automation-empty` / `git-env-ops-empty` / `layered-empty-data` / `nb-backlinks-empty` / `nb-chat-empty` / `kg-empty`，其中最后两个是"容器 + 图标"⇒ 用 `.empty-hint.is-stacked.is-fill`），全部迁进共享族；**剩下 15 个 token 分三类登记并写明理由**（① 富空态本体与它的子元素 `empty-state` / `chat-empty-inner` / `chat-empty-suggestions`；② 共享空态里的图标与标题 `*-empty-icon` / `nb-empty-title` / `nb-empty-desc` / `notebook-empty-title`；③ library-ops **插件自有面板**的 BEM 命名 `lo-*` 5 个 —— 它是像素风房间界面、有自己的样式表，不属主界面设计系统）。**为什么棘轮没看见它们**：棘轮数的是"空态类里自己写 `padding` 的处数"，而这 9 处**本来就不写 padding**（字号/颜色用的已经是一样的令牌）⇒ 方案要的"取值统一"这半**早就成立**，"实现唯一"这半没成立。**顺带踩到并修掉 4 个坑（都记档）**：① COND-1 的排除条件按**子串** `input` 写 ⇒ 真值 1 → 2（修完棘轮才第一次看见 `.input-popover-empty`）；② `count()` 按**子串**数消费者 ⇒ 数不到 `empty-hint is-stacked is-fill` 这种组合，断言写成 ≥4 会红而迁移其实是对的（改成按 token 数）；③ `.kg-empty` 有**两条**规则、我只看了一条 ⇒ LEGACY 门禁当场报"旧类又回来了"（**这正是那条断言的价值**），补成"两个类一起挂、只留铺底色那一条"；④ 迁移脚本第一版把 4 条 CSS 规则的**文件**写错（`automation-empty`/`nb-*`/`kg-empty` 在 task-center / notebook-workspace 里），并且对"同一条规则单行与多行两种写法"不兼容 ⇒ 改成**空白折叠匹配**（11 条"没找到"里有 7 条是这个原因） | **收口判据已落地为门禁 COND-1b**（不再靠人记）：扫描全部 TSX 的 className token，凡名字像空态的 token 要么**同一 className 里有 `empty-hint`**、要么在 `REGISTERED` 登记表里写明"为什么可以不挂"；另加"扫描量 > 80"的防空集守卫。变异 `COND1b-新空态类不挂共享实现` 必红（本轮变异 **43/43**）。复核：`node .preview-shot/_list-empty-tokens.mjs`（机算清单）、`node .preview-shot/_count-empty-consumers.mjs`（消费者数）、`node .preview-shot/_verify-empty-migration.mjs`（逐站点对照） |
| O-26 | ~~**真机逐面板走查的覆盖面还不完整**~~ **已关闭（第 149 轮：8/9 已量到，1 个为配置条件）**  **第 149 轮：CI-CD 的真相查清了，O-26 可以收口** —— `PanelSidebar.tsx:199` 写着 `{effectiveTab === \"cicd\" && cicdEnabled && (<CicdPanel />)}`，而 `cicdEnabled = !disabledPlugins.includes(\"@codem/ui-misc\")`（`App.tsx:518` / `PanelSidebar.tsx:64`）⇒ **右侧栏那个 CI-CD 页签只在这个插件没被禁用时出现**；本机看不到它，是因为**用户设置里禁用了 `@codem/ui-misc`**（用户配置的结果，不是缺陷，也不该由走查去改用户的插件开关）。它的数据来自 GitHub（`listWorkflowRuns`，`core/cicd`）⇒ 即便开着也需要仓库的 Actions 数据。**故这一格如实定性为「配置/数据条件不具备」并关闭 O-26**：9 个目标里 **8 个已在装机版上量到读数（全部干净：无名按钮 0、间距例外 0、控制台 0）**，第 9 个（CI-CD）的显示条件已精确到代码行并由用户配置决定。 | 走查机制（`.preview-shot/ui-walk-r72.mjs`：148 个入口 + 护栏 + 逐条指纹）已经跑通，但**目标清单里这几个面板没有真机读数**：手机连接 / 微信 / 轨迹 / 文件浏览器 / 人设 / 提示词调试 / CI-CD / 模型档案 / 上下文监视。**如实标注**：不是「走查发现它们有问题」，而是**根本没走到** —— 清单里没有它们的行（本条就是把这个缺口写下来，避免用「走查过了」这句话把没走的说成走了） | 逐个面板在**装机版**上走一遍（打开 → 量控件/控制台/遮挡 → 记录读数），写进机器状态表；走之前先取 `fingerprint-userdata` 快照，走完 `--compare` 确认没写库（第 121–123 轮的规矩） **第 130 轮实测（外壳级枚举）**：写了一个探针（`.preview-shot/_walk9-r130.mjs`）在**装机版**上枚举侧栏 + 标题栏 + 三个菜单 + 设置侧栏 + 任务中心页签里所有可点入口，按这 9 个关键词匹配 —— **一个都没匹配上**（这 9 个名字都不是外壳级入口）；同一轮 `fingerprint-userdata` 前后比对 **✅ 数据没动**（只读）。**结论**：它们要么在更深的层级（面板内部的页签/浮层），要么界面上用的不是这个名字（例如代码里确实有 `CicdPanel`、也有 `codem-wechat-bridge` 这个设置键）⇒ 下一步要**从对应面板内部逐层找**，而不是在外壳里找。**如实标注**：本轮**没有**拿到这 9 个面板的任何控件读数，O-26 **未关闭** **第 131 轮：改从源码定位它们的真实入口**（外壳找不到就去看代码把它们挂在哪）：`微信` → `SettingsPanel.tsx:981` 的一个设置页签「微信 ClawBot」（组件 `WechatSettings.tsx`，设置搜索关键词表里也有 `微信/wechat/clawbot`）；`手机连接` → 设置里的设置卡（`PhoneLinkSettings.tsx`，挂在 `SettingsPanel.tsx:1952`）；`模型档案` → `ModelProfilePanel.tsx`；`提示词调试` → `PromptDebugger.tsx`（界面文案是「系统提示词调试」）；`文件浏览器` → `FileExplorer.tsx`，**由「打开文件」事件触发**（不是导航入口，是右侧栏分割窗口）；`轨迹` / `上下文监视` 这两个名字在 `*.tsx` 里搜不到 ⇒ 界面上叫别的名字（要再定位）。⇒ 下一轮按这张表去走：**设置面板内逐页签**（微信/手机连接/模型档案）+ 打开一个文件触发文件浏览器 + 另找轨迹与上下文的真实入口 **第 132 轮真机读数（装机版 1.16.149，指纹前后比对 ✅ 只读）**：`微信` → 设置内的「微信 ClawBot」页签：**控件 87 / 无名 0 / 间距例外不成立 0 / 被遮挡 7 / 控制台 0-0 / 911 字**；`上下文监视` → 真实名字是「**上下文监控**」（聊天浮层 `.chat-float-panel`）：控件 2 / 全 0 / 控制台 0-0 / 正文「📊 上下文状态 23,678 / 115,200 tokens 21% 压力等级 正常」；`轨迹` → 真实名字是「**执行轨迹**」：入口点到了但**没出现可见面板**（0 控件 0 字）⇒ 需再确认它是页签还是浮层（本轮不判缺陷）；设置面板的页签表里**确认存在「人设」**（可下一轮直接量）；仍未定位：`手机连接` / `模型档案` / `提示词调试` / `文件浏览器`（后者按源码是由「打开文件」触发，本轮没做） **第 133 轮（指纹 ✅ 只读）**：取到**设置面板页签全表**（通用/外观/安全/Git/环境/工作树/知识/自动化/多模态/语音/Ollama/工具/**人设**/电脑操作/微信 ClawBot/**连接手机**/代码图谱/宠物/高级/帮助/用量统计/性能…）；`人设` 已量：设置浮层内 **26 控件 / 无名 0 / 间距例外 0 / 被遮挡 2 / 控制台 0-0 / 260 字**；`手机连接` 的真实名字是「**连接手机**」——入口点到了但**没出现可见面板**（与「执行轨迹」同一现象）；`模型档案` / `提示词调试` / `CI-CD` / `文件浏览器` 仍未定位。**仪器限制（如实记）**：读数只看「有没有出现浮层根」，看不出「点完之后设置面板内部的内容换了」 ⇒ 「连接手机」「执行轨迹」这类**内联切换**会被记成「没有可见面板」；下一轮要把内容指纹（文本长度 + 首行）纳入判据。**已量到读数**：微信 ClawBot（132 轮）、人设、上下文监控（真实名字是「上下文监控」） **第 134 轮：离线把剩下三个的入口精确定位（读挂载点）**：`提示词调试` → `SettingsPanel.tsx:1983` 在 `advancedSubTab === "prompt"` 时渲染 ⇒ 路径是**设置 → 高级 → prompt 子页签**；`模型档案` → `SettingsPanel.tsx:1211` 由 `showModelProfiles` 控制 ⇒ **设置内部某个按钮打开的浮层**（下一轮要找那个按钮）；`CI-CD` → `CicdPanel` 挂在 `PanelSidebar.tsx:200`，`App.tsx:459` 有 `showCicdPanel` 状态 ⇒ **面板侧栏里的一个视图**，入口名待定；`文件浏览器` → `FileExplorer.tsx`，由**打开文件**触发（不是导航）。检查命令：`grep -n "ModelProfilePanel\|PromptDebugger\|CicdPanel" src/**/*.tsx` **第 135 轮（指纹 ✅ 只读）**：给读数加了**内容指纹**（点前后比设置浮层的文本签名）后，`轨迹` 这一项**量到了** —— 真实名字「**执行轨迹**」，是聊天浮层 `.chat-float-panel`：**893 字**、控件 0（纯只读列表）、无名 0、间距例外 0、被遮挡 0、**控制台 0-0**，正文开头「执行轨迹 (8) 全部 助手 06:35:54 dsh-ref 目录为空…」⇒ 上一轮记的「没有可见面板」是**时序/选择器假象**，已更正。`手机连接`（真实名字「连接手机」）本轮仍无可见面板，**但这一轮它的读数不可信**：同一次运行里连「人设」都没找到 ⇒ 设置浮层当时可能没真正打开（仪器状态问题），**不据此下任何结论**。**累计已量到 4 个**：微信 ClawBot、人设、上下文监控、执行轨迹 **第 136 轮：「连接手机」重测（这次先确认设置浮层已打开，再点）**：设置浮层 `true` → 点「连接手机」→ 设置浮层**消失了**（`.settings-overlay` 变 null、控件 0）。⇒ 它**不是内联切换**（上一轮的猜测被推翻），而是**关掉设置**后把内容放到别处（或没放）。**如实标注**：本轮**没有**确认它把内容放去了哪里（现有读数只认浮层类根选择器，认不出主区域整页视图）⇒ **不判它是不是缺陷**，下一轮要加一条「主区域可见文本指纹」的判据才能定论。累计已量到 **4 个**（微信 ClawBot、人设、上下文监控、执行轨迹）；`连接手机` 归入「行为已量到、落点未知」 **第 137 轮：「连接手机」的落点查了 —— 出来一个可疑候选**：加上主区域可见文本指纹后重测，点它之后**主区域可见文本反而变少**（2826 → 1914 字，头部内容没变 ⇒ 少掉的就是设置浮层那部分），**没有任何新增内容**。⇒ **可疑缺陷候选**：点这个设置入口像是「只把设置关掉、什么都没发生」。**但不判为缺陷**：它也可能走的是原生窗口 / 外部动作 / 二维码图片（非文本，我的文本指纹看不见）。**下一步判据（写清楚）**：①用 CDP 监听 `Target.targetCreated`（点它有没有开新窗口/页面）②点后统计主区域的 `img/canvas/svg` 等**非文本内容**有没有增加 ③若两条都是否 ⇒ 才定性为缺陷并修。**本轮指纹（第 136 轮那次）✅ 只读** **第 138 轮：把上一轮那个「可疑缺陷候选」撤了（读源码定性）**：`连接手机` 全仓只出现在三处 —— `SettingsPanel.tsx:985` 的**设置侧栏项**、`SettingsPanel.tsx:573` 的设置搜索关键词、`PhoneLinkSettings.tsx:124` 的卡片标题「连接手机（LAN）」；也就是说它**本该是设置面板内的内联页签切换**（浮层不该关闭）。所以第 136/137 轮看到的「点完设置浮层消失、主区域文本变少」**不是产品行为，而是我自己的点击打偏**：很可能是命中坐标落在**遮罩层**上（模态遮罩一点就关），和之前那次 `overlayArtifact` 是同一类仪器假象。⇒ **撤回候选缺陷**，并记下通用修法：**点击前先做命中自检**（`document.elementFromPoint(x,y)` 必须就是目标元素本身，否则不点/记为不可点）—— 这条要并进走查的点击路径。**本轮不声称「连接手机」有问题** **第 139 轮：命中自检把它证实了（不是推理，是量出来的）**：打开设置后对「连接手机」侧栏项做 `document.elementFromPoint(中心点)` —— 该点的**最上层元素是 `.settings-overlay` 容器本身，不是那个侧栏项**（`hitSelf` 只有用宽松的 `overlay.contains(item)` 才算真）⇒ **上一轮那次点击确实是打偏了**，第 136/137 轮的「可疑缺陷候选」正式作废。**已写进清单的通用修法**：走查/探针点击前必须 `elementFromPoint(x,y)` 命中目标本身（`top === el \|\| el.contains(top)`），否则**不点**并记为「不可点」——这条能一次防掉这一类假缺陷 **第 141 轮：命中自检上线后第一次实战**：①`连接手机` 被自检**拒点** —— 该点最上层是 `DIV.settings-footer`（设置侧栏要滚动才能露出这一项，中心被底栏盖住）⇒ 这次**没有再被误记成「点了没反应」**，而是如实记为「不可点（命中自检未过）」；这正是第 140 轮那条修法的价值：**假缺陷在产生之前就被挡住**。下一步：先把设置侧栏滚到底再走它。②`提示词调试` 在「高级」页签里**没找到入口**（按源码它在 advancedSubTab 为 prompt 时渲染；可能是子页签的名字/图标与我的正则不匹配）⇒ 下一轮先把「高级」里的子页签**全列出来**再点。**指纹本轮 ✅ 只读** **第 142 轮（指纹 ✅ 只读）：又量到 1 个，并破了两项的关键**：①先把设置侧栏**滚到底**，再对「连接手机」做命中自检 → 通过 → 点击量到：设置浮层内 **31 控件 / 无名 0 / 间距例外 0 / 控制台 0-0 / 694 字** ⇒ 它本来就是**设置内的内联页**（与源码一致），第 136/137 轮那次「点完设置消失」**确认是我的误点**（已作废）。②「高级」页签量到（36 控件 / 全 0 / 676 字），且**列出了它的子页签**：智能体 / 心跳 / 重试 / **提示词** / 分层设置 / 纠偏模型 / **Agent Profile** ⇒ **提示词调试 = 设置→高级→提示词**、**模型档案 = 设置→高级→Agent Profile**（下一步直接走这两个）。**累计已量到 5 个**：微信 ClawBot、人设、上下文监控、执行轨迹、连接手机；待走：提示词、Agent Profile、CI-CD、文件浏览器 **第 143 轮（指纹 ✅ 只读）：再量到 2 个（累计 7 个）**：`提示词调试` = 设置→高级→**提示词**：**42 控件 / 无名 0 / 间距例外 0 / 控制台 0-0 / 17520 字**（内容量最大的一个）；`模型档案` = 设置→高级→**Agent Profile**：**36 控件 / 无名 0 / 间距例外 0 / 控制台 0-0 / 260 字**。两者都干净（无缺陷迹象）。**只剩 2 个**：CI-CD（`CicdPanel` 挂在 `PanelSidebar`，触发方式待找）、文件浏览器（由打开文件触发） **第 144 轮：查 CI-CD 触发口时抓到一处真·死代码并修掉** —— 全仓 `showCicdPanel` **只有一处命中**（`App.tsx` 里的声明），**既没人读也没人写** ⇒ 已删除该状态（无行为变化）。而 `CicdPanel` 是在 `PanelSidebar.tsx:200` 渲染的 ⇒ CI-CD 的真实入口在**面板侧栏的某个视图**里，下一轮从 `PanelSidebar` 的调用点找它。`文件浏览器` 仍需**打开一个文件**才能触发 **第 145 轮：最后两个面板的入口也定了 —— 它们都在「聊天右侧栏」（`PanelSidebar`）里**：`CicdPanel` 由 `PanelSidebar.tsx:200` 渲染，而 `PanelSidebar` 是 `ChatPanel.tsx:1244` 用 `showRightSidebar` 控制的**右侧栏**（不是设置面板、也不是任务中心）；`FileExplorer` 同理 —— `App.tsx:1151` 的注释写着「监听文件打开事件（来自侧边栏等所有文件浏览器），自动展开右侧栏显示分割窗口」⇒ **打开一个文件就会在右侧栏出现**。**所以下一轮一个探针就能收尾**：打开聊天右侧栏 → 在里面找 CI/CD 区块量它 → 再触发一次「打开文件」量文件浏览器。**累计已量到 7 个**，只剩这 2 个 **第 146 轮：摸了右侧栏但没展开（如实记）** —— 右侧栏元素在（`right-sidebar right-sidebar-collapsed`，即**折叠态**），开栏入口是「展开右侧栏」（`.right-sidebar-toggle-col`）与「侧边面板」（`.agent-toggle`）；点「侧边面板」对右侧栏无作用，点「展开右侧栏」**被命中自检拒掉**（该点最上层是 `DIV.`（类名为空））⇒ **这一轮没拿到 CI-CD / 文件浏览器的读数**。**下一步**：点那个切换器**内部真正可点的子元素或边缘**（而不是它的几何中心），再量 `.right-sidebar:not(.right-sidebar-collapsed)` 里的 CI/CD 区块。**指纹本轮 ✅ 只读** **第 147 轮：右侧栏展开了，量到读数** —— 右侧栏（展开态）控件 **5**、无名 **0**、间距例外 **0**、控制台 **0-0**、正文 **1104 字**；CI/CD 标志 **false**、文件树标志 **true**｜开头：文件 浏览器 .art-inbox .codem-cache .codem-snapshots .codem-tool-results .deepseek-ha **第 148 轮：CI-CD 仍没出现（如实记）** —— 右侧栏展开后它自己的控件只有 4 个（收起右侧栏），逐个按护栏 + 命中自检点过之后，**文本里始终没有 CI/CD / 流水线 / 工作流标志** ⇒ 判据是「**本机没有工作流数据 ⇒ 该区块不渲染**」（不是点不到）；要在真机上量它，得先用副本库造一条工作流记录。**至此 9 个目标里 8 个已量到、CI-CD 属"数据条件不具备"** **第 148 轮补充两条现场事实**：①右侧栏自己的页签**只有两个**：「文件」（active）与「浏览器」 ⇒ 这一版右侧栏里**没有 CI-CD 页签**（CI-CD 要么只在有工作流数据时渲染，要么挂在别处）。②**仪器教训**：我的探针第一下点的是「**收起右侧栏**」—— 护栏**没拦**（它的文案里没有破坏性/全局状态类词），结果**自己把右侧栏收起来了**，后面三个控件随即命中自检失败（侧栏已折叠）。⇒ 护栏该补一类 **layout toggle**（展开/收起侧栏这类改界面布局的入口）  |
| O-24 | ~~**「写了但没接线」现在有门禁了**（第 109 轮新增，这道闸门本身也刚被查过一次假红）~~ **已关闭（第 127 轮）** （第 110 轮 O-22 收口即达成：白名单两侧相等 + 两条已知答案对照 + 变异 5/5） | 工具 `tools/audit/reachability-scan.mjs`（模块解析 + 可达性 + **两条已知答案对照**）落地到仓库；白名单 `tools/audit/reachability-allowlist.json` 把 39 个不可达文件逐条登记并写清类别。**两道闸**：用例 `reachability-gate.test.ts`（REACH-1…4）+ CLI `npm run audit:reachability`（**audit 13 → 14 道**）。本轮还查出并修掉门禁自己的**偶发假红**：第一版把"未跟踪文件"算进范围，而全量测试里有用例会往 `src/` 写临时产物 ⇒ 扫描偶尔把它们当"新出现的不可达文件"；现在只算 git 跟踪的文件 | 用它继续烧 O-22 的两处待判（`storage/sync-engine.ts`、`components/RegenerateModelPopover.tsx`）：接线或如实标注 —— 无论怎么判，白名单里的理由都要跟着改 |

| O-9 | ~~更新器下载失败时没有任何重试与可诊断信息~~ **已关闭（第 82 轮）** | 见下面"已关闭"里的 C-11：有界重试 + 可读错误已落地并配门禁/突变 | — |
| O-10 | **走查/探针会改用户状态** **已标准化（第 117 轮）** | 本轮探针点「切换执行模式」把 mimo-gui 的执行模式从"本地处理"改成了"新工作树"（`codem-project-execution-modes = {"C:\\mimo-gui":"git_worktree"}`）。已还原：关应用 → 用引擎 CLI 直写设置为 `current_workspace` → 重启后标题栏显示「本地处理」（工具 `.preview-shot/_exec-mode-setting.mjs`，含只读回读核对）。**教训**：探针要"点会改状态的东西"时，先记下原值 **第 117 轮把它变成代码 + 判据**：新增 `tools/audit/probe-guard.mjs` —— ①`classifyClick(control)` **点之前先判**：窗口控制（靠 `inTitlebar` **上下文**判，不按文案一刀切，这正是第 94 轮踩的坑）/ 破坏性动词 / 改全局状态 / 真提交，四类默认拒绝，且拒绝时**带上为什么**（写明是哪一次事故）；面板自己的「关闭」照常放行（走查本来就要开关面板）。②`createProbeGuard()` **改之前先登记怎么还**：`record(说明, 还原函数)`，结束时（正常/抛错/中断）**逆序**执行、幂等、单个失败不吞（记进 errors 继续还剩下的）。自证 `tools/audit/probe-guard.selftest.mjs` **21 条**（进 `npm run audit`，现在是**第 15 道**），变异 `.preview-shot/mutate-probe-guard.mjs` **7/7**。**真机验证**（装机版 1.16.149，把真实 DOM 上的控件喂给护栏）：标题栏「最小化/最大化/关闭」→ 拒（window-control）、「切换执行模式/切换主题/切换终端」→ 拒（global-state）、侧栏「新对话」→ 拒（global-state）；「文件/视图/帮助/搜索/设置/技能/MCP/记忆」等面板入口 → 放行。走查脚本 `ui-walk-r72.mjs` 里那份本地 `DENY` 正则**已删除**，改成调用护栏（名单从此只有一份、且有判据） | —（护栏已就位：新走查/探针一律 `import { classifyClick, createProbeGuard }`；走查前先复位浮层，改动前先 `record`。**残留**：老的 `.preview-shot/*` 一次性脚本还没逐个迁到护栏——它们大多只读，遇到会改状态的再迁）；**第 121 轮补**：这条护栏漏了一个**会写库**的入口 —— 设置→「运行登录测试」在建/删一行 `accounts`（第 118 轮读数里那条唯一 warning 就是它，已用指纹实证：点它会让 WAL 的 sha256 变），已补 `data-write` 规则（真机 DOM 上被拒入口 9 → 30 个，其中 21 个属这类）**并加了结果侧判据** `tools/audit/fingerprint-userdata.mjs`（走查前后比主库/WAL/shm 的 sha256，`--compare` 有变化即退出码 1；对照相已证不误报） |
| O-16 | ~~启动时「密钥解不开」只有控制台 warn，没有用户可见面~~ **已关闭（第 89 轮）** | 见下面「已关闭」里的 C-22：钻取把它确认成缺口，本轮修好并在装机版上量到提示条 | — |
| O-17 | ~~还有「发现类」消息没走 advisory？~~ **已关闭（第 104 轮，清零）** | 从 217 处上报点起步：第 90 轮先登记全部现状（94/123），第 100 轮把读侧事件从 persist 通道搬走（131/86），**第 104 轮把剩下 86 处全部逐处分诊完（217/0）**。判据（写在登记表 `_how_to_triage`）：真失败 ⇒ persist（写盘）/ action（功能没生效）；自检、普查、对账、自愈的**发现** ⇒ advisory。本批 3 处改通道（两处 todo 勾选分支 → action；compaction 载荷不合契约 → advisory），并修掉一处**假话**（`sessionLog.rebuildIndex` 的 extra 写着"已回退旧路径重试"，而回退第 17 轮就删了）。棘轮 `PENDING_BASELINE` 86 → **0** | — （判据已达成）。维护方式：新上报点会被 `scan-report-sites --check` 要求登记，登记成 pending 会被棘轮拦下 ⇒ 必须当场分诊 |
| O-18 | ~~提示条文案会出现重复标点~~ **已关闭（第 90 轮）** | 见下面「已关闭」里的 C-23：在拼装的唯一出口折叠重复句末标点，判据含「省略号不许被折叠」的反向对照 | — |
| O-19 | ~~附件正文的「真机端到端」还差最后一格~~ **已关闭（第 92 轮）** | 见机器状态表「附件正文的端到端」那一行：装机版 1.16.140 上点开后 **1 秒内正文出现**（指纹与库一致）。之所以之前一直是空的，是因为 C-27（`content` 被读改写抹成 NULL）—— 修掉之后这条就通了 | — |
|  O-21 | ~~「≥50 行的生产文件行覆盖 0%」~~ **已关闭（第 103 轮，清零）** | 量法：`node .preview-shot/_coverage-inventory.mjs`（读 `coverage/coverage-summary.json`）。**进度：13 → 9 → 6 → 4 → 2 → 0**。清法分两类：**补测试**（`slots/declare-slots`、`llm/run-status-tracker`、`theme/contrast-checker`、skill-creator 4 个脚本、`knowledge/exporter\|importer`、`knowledge/pptx-importer`、第 103 轮的 `knowledge/graph-extractor`（10 条）与 `knowledge/ppt-generator`（13 条））与**删死代码**（第 102 轮：`llm/processor.ts` 孤儿实现 + `llm/session.ts` 的 `SessionManager` 类——死代码不该去"提高覆盖率"，该删）。**第 103 轮实测：≥50 行的生产文件 179 个，其中行覆盖 0% 的 0 个**；<20% 的 28 个 / 2412 行（这一类仍是待办，但**不在本项判据里**）。全局行覆盖 53.67% → **57.08%** | — （判据已达成：0 个）。后续若想继续推进，看「<20% 的 28 个文件」与全局阈值，而不是本项  |
| O-20 | ~~**「小于 24×24 的真控件」只剩 1 处真不达标**（第 94 轮量的清单 → 第 95 轮修完大半）~~ **已关闭（第 127 轮）** （第 95 轮修、第 96 轮复量 spacingOk 全 True；第 118/123 轮走查再确认「间距例外不成立 0」） | **已修（第 95 轮）**：`.api-key-toggle`（36×19）、`.session-recovery-close` / `.usage-stats-close`（22×22）、自动化触发器那个空 label 里的复选框（补 `aria-label` + 24×24 命中区），并在装机版 1.16.142 上复核它们都不再出现在「小于 24」清单里。**剩下的都按 WCAG 2.5.8 的间距例外算合规**（中心距实测：13×13 复选框 42px、`icon-md 16×16` 114px、语音 160×16 62px、548×16 67px、宠物滑杆 171px、560×14 334px、git 输入 548×18 27px、内联链接 155px、插件装饰方块 45px）。**只剩 1 处间距不成立**：设置 → 外观的 `INPUT.sp-flex-fill`（502×16，与最近目标只隔 **6px**） | 给 `sp-flex-fill` 那一行补纵向间距（或给输入框 24 下限），然后重跑走查确认 `spacingOk` 全 True；**另外**：命名侧已加棘轮（`scan-labeled-inputs.mjs`，基线 0，只许降不许升） |




## 三、本轮**已关闭**的项（附判据）

| # | 项 | 判据（可复核） |
| --- | --- | --- |
| C-1 | 反馈有**两条写路径**：遗留 `MessageStorage.saveFeedback`（引擎 `feedback.set`，**5 列**、不写域镜像）+ 域写（**9 列**）；两者并存时 5 列那条会把 `note`/`version`/`created_at`/`updated_at` 抹成 NULL，且写进去读不到 | 渲染侧与**引擎侧**都删掉了：`src/core/storage/message.ts` 的 `saveFeedback` / `feedbackCache` / `invalidateFeedbackCache`，`src-tauri/codem-db` 的 `feedback.set/get/delete`（命令清单 + 派发分支 + `config.rs` 实现三处）。门禁 `src/test/feedback-single-write-path.test.ts` 6 条（FB-1…FB-6，含**全生产源码树扫描**与**引擎命令清单**两处结构判据）+ 引擎用例 `message_feedback_goes_through_generic_crud`（9 列写、覆盖不产生第二行、取消 = 删行、`CHECK` 与外键仍然生效）。**7 处突变全部被抓**（`.preview-shot/mutate-feedback-single-write.mjs`） |
| C-2 | 覆盖率阈值**从来没有生效过**（provider 未安装 ⇒ `npm run test:coverage` 直接 `MISSING DEPENDENCY`） | 装 `@vitest/coverage-v8` + 先量后定棘轮（见 O-4）+ 对账工具 `tools/audit/coverage-baseline.mjs`（`--check` 在 `npm run verify` 里跑）+ 基线文档 `tools/audit/coverage-baseline.md`。顺带修掉一条**带覆盖率跑就会超时**的用例（`EVENT-TYPE-WRITES-3` 要重扫 821 个文件，默认 5s 不够 —— 给到 60s 并写明这是度量开销不是放宽断言） |
| C-3 | 三处注释与实现对不上（`CHAT-022b/023b` 说"读旧库"、`C4-5` 说"顺序碰巧才对"、`store.ts` 说"这里同时发两条写"） | 注释按当前实现重写，并把**已经不存在的东西**写成"已删除 + 为什么"；`C4-5` 的断言从"顺序碰巧"改成"列集合就是 9 列"（唯一的写者给的） |
| C-4 | 首屏会读的小表**没进预取清单**（`message_feedback` / `delegation_tasks` / `attachments`） | 三张表都进了 `HOT_DOMAIN_TABLES`，每条都带**真机可见的症状**注释；清单里同时写明哪些表**故意不预取**（`notebook_chunks` 每行带 embedding） |
| C-5 | 侧栏未读徽标 / 聊天待办面板是**死代码**（读的字段全仓无写入点） | 新增已读水位（`core/session/session-read-state.ts`，存 settings）+ `latestTodoListForSession` 三态读；门禁 `session-unread-badge.test.ts` 8 条、`audit-followups-todo-and-list.test.ts` 6 条；装机版实测：子会话显示"5 条新消息"（水位 0 / 库里 5 条）、正在看的会话无徽标 |
| C-6 | **确认框根本没弹、不可逆动作照做**（真机走查抓到） | dialog 插件把 `window.confirm` 换成异步调用（返回 Promise 恒为真）+ ACL 没放行 confirm ⇒ `if (!confirm(x)) return;` 永远继续。修法与判据全在 O-3 那一行（含 13 处站点清单与 6 处突变） |
| C-7 | 走查覆盖：**100 个入口**（此前只走过其中一部分面板） | `docs/ui-walk-round72.md` + `.preview-shot/ui-walk-r72.json`；19 个外壳入口 + 56 个设置页签 + 25 个任务中心页签逐个"点开 → 量面板 → 记控制台 → 复位" |
| C-8 | **上下文面板自相矛盾**：同一屏写着 `23,678 / 115,200 tokens 21%` 与「压力等级：临界」+「🔴 上下文即将满」 | 根因是同一件事两套口径：进度条用模型侧口径（可见 → 裁剪陈旧工具结果 → 按优先级选进"真实窗口 × 0.9"），压力等级却另调 `getPressureLevelFromMessages`（另一套分母、且不裁剪不选择）。修法：阈值收成唯一实现 `pressureLevelForRatio` + `summarizeDisplayPressure`，`ContextMonitor` 的 `pressure` state 整个删掉、等级由**进度条那两个数**现场导出。门禁 `context-monitor-pressure.test.ts` 5 条 + **5 处突变全被抓**（`.preview-shot/mutate-context-pressure.mjs`） |
| C-9 | **长跑稳定性只有一次性读数、没有门禁**（目标里的"内存与句柄不涨"） | 新增 `.preview-shot/stability-longrun.mjs`：连续采样（堆 / DOM / codem 工作集 / **句柄数** / 进程树），比较**前 1/3 与后 1/3 的中位数**并设绝对上界；**没有负载就直接判失败**（防"量的是空闲进程"）。实测 8 分钟 / 19 次采样 / 114 次面板切换：堆 41→42MB、DOM 844 恒定、句柄 362→364、进程树 1266→1282MB ⇒ **通过**。（`O-7` 因此关闭；"跑一夜不漏"仍不是这条能证明的，脚本注释里写明了边界） |
| C-10 | **两份关键文档没进仓库**（`.gitignore` 的 `docs/*.md` 把它们吞了） | 第 81 轮 `git ls-files docs` 实查：`docs/GAP-LIST.md` 与 `docs/ui-walk-round72.md` **未跟踪** —— 本机磁盘上有、仓库里没有。后果有两条：① CHANGELOG / GAP-LIST 里"报告见 `docs/ui-walk-round72.md`"指向不存在的文件；② 别人 clone 下来 `docs-current-gap-list.test.ts` 的 DOCS-1 会**直接失败**（清单不存在）。修法：`.gitignore` 加 `!docs/GAP-LIST.md` / `!docs/ui-walk-round72.md` 白名单 + 新增 **DOCS-6** 判据（`git check-ignore -q <path>` 的**退出码**：0=被忽略、1=没被忽略；⚠️ 不能用 `-v` 的 stdout 判断 —— 它会把 `!` 取反规则也打出来，第一版因此把"已放行"误判成"被忽略"）；反向对照用刻意忽略的 `.preview-shot/`。另附"模拟 clone"检查（`.preview-shot/_gate-fresh-clone.mjs`）：clone 后 27 份顶层 docs、其中 7 份名字像"计划/缺口"且**全部带横幅并链回 GAP-LIST.md** ⇒ 门禁在 clone 条件下同样通过 |
| C-11 | **更新下载一次失败就放弃、错误不可读**（第 72 与 81 轮各复现一次） | 新增 `src/core/update/update-retry.ts`：**有界重试**（只对可恢复的传输类错误，最多 3 次，退避 800→1600ms，上界硬性夹到 5）+ **可读错误**（`describeUpdateError`：原因 + 下一步 + **保留原文**）+ 界面显示「下载中断，正在重试（第 n/3 次…）」。**签名/校验类错误一次就抛**（重试没有意义，还会把严重问题稀释成"网络不好"）；不认识的错误默认不重试。门禁 `update-download-retry.test.ts` 7 条 + **6 处突变全被抓**；⚠️ 其中两条判据是突变验证逼出来的（纯签名串测不出优先级 ⇒ 补混合串；组件接线只判"文本出现过" ⇒ 改成锚行首 `await`） |
| C-12 | **提示框（`alert`）没走统一入口**：失败时"点了没反应"，且无上报 | 生产代码 **15 处**裸 `alert(...)`（9 个文件）全部迁到 `void alertDialog(...)`（失败进上报通道、横幅可见）。**刻意不迁**技能自带脚本 `src/core/skills/skill-creator/scripts/*.ts`（运行环境不保证有 WebView，套 dialog 反而是错的），这条边界写在门禁注释里。新增 **NC-6**（生产源码不许再出现裸 `alert(`；只扫调用，`alert:` 键与 `AlertDialog` 标识符不算）+ **NC-7**（`alertDialog` 在弹不出来时必须上报且带原文）；现有突变脚本扩到 **8 处全被抓**（新增"裸 alert 复活"与"alertDialog 失败不上报"）。迁移工具 `.preview-shot/migrate-alert-dialogs.mjs`（先干跑列清单、再 `--apply`） |
| C-13 | **皮肤卡片键盘完全用不了**（UI/UX 走查的可访问性那一类） | `SkinSelector` 原来是 `<div className="skin-card" onClick=…>`：无 `role`、无 `tabIndex`、无键盘处理 ⇒ Tab 跳过、Enter/Space 无反应、读屏不念成控件（皮肤是用户可感知功能，键盘用户却选不了）。改成 `<button type="button">` + `aria-pressed` + 网格 `role=group`。门禁 `ui-a11y-skin-avatar.test.ts` A11Y-1/2（带反向对照）；**装机版复核**：3/3 是 BUTTON、3/3 可聚焦、3/3 带 aria-pressed、恰好 1 个选中、名字互不相同 |
| C-14 | **走查度量把"名字来自后代图片 alt"的控件误报成无名** | 走查报「头像预设 50 个无名按钮」，实为度量只看 `textContent`（`<img alt>` 不算）⇒ 误报。度量侧修正：`audit-walk-lib.mjs::__name` 按 aria-label → aria-labelledby → title → 后代 `img[alt]` → textContent 取名；门禁 A11Y-4 守着这个顺序。**顺带修的**：这轮在模板字符串里写反引号把整个 `audit-walk-lib.mjs` 弄成语法错误，而**测试套件全绿**（没有用例 import 它）⇒ 新增 `preview-shot-syntax.test.ts`（PS-1 用 esbuild 逐文件解析，**711ms** 而不是 `node --check` 子进程的 **76s**；PS-2 真的 import 共享库并断言导出）；查出并隔离 4 个语法已坏的旧脚本（见 O-14） |
| C-15 | **纯图标按钮没有可访问名**（关闭按钮 + 折叠侧栏图标条，共 39 处） | 走查（度量修好之后）152 个入口、控制台报错 0；"无名按钮"降到 8 个面板，看 HTML 只有三处关闭按钮。补静态规则全仓扫后**又发现扫描器自己在漏报**（`onClick={() => …}` 的 `>` 把非贪婪正则截断 ⇒ 报 55、真实 195），修正后修掉 **31 个图标关闭按钮**（`aria-label="关闭" title="关闭"`，只动"只有图标"的）+ **8 个折叠侧栏图标条**（名字取自紧邻 `<TooltipContent>`；Tooltip 不能替代可访问名）。门禁 `icon-button-a11y.test.ts` 4 条（关闭类严格 0 / 折叠侧栏严格 0 / **其余棘轮基线 156** / **给扫描器的反向对照**）；规则与审计脚本共用唯一实现 `tools/ui-audit/icon-button-scan.mjs` |

|  C-16 | **确认框在真机上一直弹不出来**（真正的根因：注入的 `window.confirm` 指向不存在的命令） | 读依赖源码逐字核对：`tauri-plugin-dialog` 2.7.2 的 `src/init-iife.js` 把 `window.confirm` 指向 `plugin:dialog\|confirm`，而同一 crate 的 `src/lib.rs` 只注册 `open`/`save`/`message` ⇒ **那个命令不存在**（`permissions/confirm.toml` 自己写着 `allow-confirm` 是 DEPRECATED、「now an alias to allow-message」）。1.16.125 的改动只把"不问就做"变成"安全地拒绝"，**弹框从未弹出来过**。修法：真机走插件 JS API（`@tauri-apps/plugin-dialog` 的 `confirm()`，打的是已注册的 `plugin:dialog\|message`），优先级 = ①插件 JS API ②`window.confirm` 同步布尔 ③都失败则 fail-closed + 上报；①失败会回退到②。门禁 9→**11 条**（NC-8/NC-9）。**装机版实测**：控制台 error 0 + `#32770` 对话框窗口出现 + 主窗口被 disable；PostMessage 回车后恢复  |

| C-18 | **沙箱开关的 fail-open**（同一形态系统扫描出来的唯一安全项） | `isSandboxAclEnabled` 原来 `catch { console.warn; return false }`：用户打开的沙箱在一次读失败后**静默失效**，设置面板开关**仍显示已开启**。修法：记住上次成功读到的值 + 失败沿用 + 走上报通道（`sandbox.readSetting`）+ 无历史值时按默认关闭并如实说明。**扫描工具**：`node tools/audit/scan-fail-open-guards.mjs`（15 处线索 → 12 能力探测 / 2 读取型 / **1 安全项**）；**闸门**：该 14 处已定性入白名单，`npm run audit` 新增 `audit:fail-open`（新增未定性 ⇒ 红、白名单过期 ⇒ 红），门禁 `fail-open-guard-gate.test.ts` 5 条 + 变异自证 6/6 |

| C-19 | **4 个语法已坏的历史脚本：改动是否真的生效**（O-14） | 判据写的是**效果**、不是当年的代码原文（这点第一次跑就踩了坑：拿脚本里的原字符串当针，报出 4 处「没落地」，全是假警报 —— 那段接线后来重构成了更好的形态，效果还在、只是名字变了）。核到 7 项全落地：`src/App.tsx` 里 `ensureSecretsHydrated`（启动解封，现形态是单飞入口）+ `migrateProviderKeysToSealed` + `reclaimSealedPlaintextResidue`；`SecretStorageSetting.tsx` 里 `secretStorageStatus` + `status.unreadable`（「本账户解不开」显示在设置→安全）；`secret-store.ts` 里 `isSealedKeyUnreadable` 真被汇总使用（不是只记不读）；CHANGELOG 有 1.16.101 条目 + 「凭据普查」+「未跑成」那条纪律；`docs/CREDENTIALS-PLAN.md` 有阶段 0；设计体系文档有第 27/28 波行与 `zindex-raw`/`css-class-duplicate`/`spacing-raw` 三条规则。工具：`node .preview-shot/verify-quarantined-effects.mjs`（7 项检查、exit 0）。**顺带发现**：O-16（启动时「解不开」缺用户可见面，待确认） |
| C-20 | **设计体系文档里 7 处陈旧的「（本轮）」标记**（历史清单自相矛盾） | 现场：`docs/UI-DESIGN-SYSTEM.md` 的「已完成的波次」清单里，第 28 / 34 / 39 / 40 / 41 / 42 / 46 波**同时**写着「（本轮）」—— 每波写时都对，但降级靠「下一波脚本顺手改」，漏一次就永久留下假「本轮」，读者看不出当前波次。同一节还有第二处：§7 标题写「第 45 波后」而它自己的表已引到第 52 波（**自相矛盾的波次声明**）。修法：清掉 7 处标记 + 在清单开头写死约定（历史记录一律不标本轮）+ §7 标题不再挂波次号（不猜数字）。门禁 `docs-wave-freshness.test.ts` **3 条**：DOCW-1a（无「本轮」残留）/ DOCW-1b（约定必须在）/ DOCW-2（§7 标题声明的波次号不得小于全文最大波次号）；DOCW-3 用三个坏样本（加回一个「本轮」、把标题挂回小波次号、删掉约定）证明判据会红，并用真文档证明它不是恒红。工具：`.preview-shot/fix-stale-current-wave.mjs`（幂等，`--apply` 才写盘） |
| C-21 | **「发现」被套进「失败」模板**（隔离钻取在装机版上量到原文） | 现场（装机版 1.16.134，副本库+`CODEM_DB_PATH`）：横幅「安全提示：设置里存在明文凭据：设置里存在疑似凭据 1 处。**该功能本次不可用，请重试或检查日志**。」；控制台「[PersistFailure] maintenance.invariantAudit.new **写盘失败**（第 1 次）：不变量审计：本次新产生 39 条缺口 —— 本次改动只存在于内存，重启后可能丢失。」。四句假话：「不可用」（普查刚跑成功）、「请重试」（再跑一次还是同样发现）、「写盘失败」（没有写盘失败）、「改动只在内存」（没有改动）。修法：新增第三条通道 **advisory**（横幅 `提醒（area）`+建议、控制台 `[Advisory]`+warn、第三种颜色、后缀改「同类提示 N 次」），四个「发现」站点整条搬过去（凭据普查 / 自检新缺口 / 结构异常 / 索引落后已自动补回）；真失败一个没动。**失败侧的开头也修真**：9 处维护类失败补 `title`+`consequence`（裁剪/完整性检查/空间回收/自检没跑成原来印「写盘失败」「数据保存失败」），并让控制台标签优先用 title。**顺带**：`looksSealed` 放宽到 hex 或 base64 载荷 —— 密文不再被说成「明文凭据」。判据：`persist-advisory-channel.test.ts` 7 条 + CENSUS-6 + EVENT-TYPE-WRITES-8 加严；`mutate-advisory-channel.mjs` **11/11**（含过度归类那种反向变异）。**钻取纪律**：只读真库、只写副本，前后主库 sha256 一致，结束后用正常环境重启恢复现场 |

| C-22 | **密钥解不开时界面上一条提示都没有**（O-16 的结论） | 钻取（第 88 轮）确认：密钥解不开 ⇒ 这些 provider **用不了**，而界面**无提示**，唯一可见处是**主动**进「设置 → 安全」。修法：决策逻辑抽到 `credential-startup-report.ts`（可单测），四种状态按语气归位 —— 解不开 ⇒ **失败**（`凭据：本机解不开已保存的密钥` + 「重新填写 API Key」）、明文没封存成 ⇒ **失败**（说明仍是明文 + 建议轮换）、本机没有系统加密 ⇒ **提醒**、字节残留没回收 ⇒ **提醒**；一切正常 ⇒ **零上报**。判据 `startup-credential-visibility.test.ts` 5 条 + 变异 **8/8**；**装机版 1.16.136 实测**（见机器状态表那一行）：同一钻取下界面出现 `persist-alert is-action` 提示条（改前没有），正常库启动零提示、error/warning/exception 0 |

| C-23 | **提示条文案的重复句号**（真机原文「…有同一处状态与入口。。」） | 现场：装机版 1.16.136 的钻取里提示条末尾印出「。。」——拼装是「标题 + reason + 后果 + 次数后缀」四段拼的，每段都可能自带句末标点。修法：在**唯一出口** `composePersistAlertText` 过一遍 `collapseDuplicatePunctuation`（只折叠连续重复的同一句末标点与中英混排残留，**不动**省略号 `……`）。判据 ADV-8（含「省略号不许被折叠」与「单个句号/问号照旧」的反向对照），变异自证里 M-系列覆盖 |

| C-24 | **无名图标按钮清零 + 扫描器第四类误报**（O-4 ①） | 两类误报：①非字面量表达式（{item.title}、{S.ollama.save[lang]}）会渲染文字而旧判据只看字面量；②嵌套花括号里的字面量被嵌套不匹配的正则在第一个 } 处截断 ⇒ 扫不到（{a === b ? <><Clock size={12}/> 恢复中...</> : …}）。两类合计 **39 处误报**（报 51、真问题 12）。**这些按钮本来就有可见文字，加 aria-label 会盖掉读屏要念的文字**（负优化）⇒ 一律不动；只修真正无名的 12 处（气泡分支/重新生成取旁边 Tooltip 的同一 i18n 串、目标栏保存/取消、笔记删除、PDF 上下页与缩放、审核框关闭、设置搜索清空、会话删除、小队移除成员）。门禁：A11Y-ICON-1/2 严格 = 0、**棘轮 51 → 0**、新增 A11Y-ICON-5/6（含「整段是 JSX 图标的播放/暂停仍必须被报」的反向对照）。审计脚本 audit-icon-buttons.mjs 报 **0 处** |

| C-25 | **更新器重试与可读错误**（O-13 的真机端到端） | 1.16.127 修的「有界重试 + 可读错误」在此前 **6 次**真机升级里都没被触发过（每次都下载干净）。第 91 轮 1.16.137 → 1.16.138 的升级**真的被掐断三次**：按钮依次显示 `下载中断，正在重试（第 2/3 次，等了 1s）…`、`（第 3/3 次，等了 2s）…`，控制台两次 `[updater] 下载第 1/3 次失败（可重试）：error decoding response body`，最后给出**可读失败**：原因（网络把安装包掐断） + 下一步（重试或去 Release 手动下载） + **保留原文**。再点一次即成功升级。**这条缺口彻底关闭**（真机端到端，不是只靠门禁与突变） |

| C-26 | **附件点开看不到正文**（O-2 的结论，且比原判断更严重） | 原判断是「真机没有附件数据（`attachments` 0 行）⇒ 量不到」。第 92 轮改用**钻取**：复制库 + **复制权威日志**（第一次只复制库，侧栏「暂无对话」—— 会话/消息的权威副本是 `<dataRoot>/sessions/*.jsonl`，这个坑记在工具注释里）+ `CODEM_DB_PATH` 启动**装机版**，把一条真附件挂到「当前可见消息」上。结果：**元数据看得见**（`[message-attachments] live-attachment.txt 61 B`，name/size 都在），**点开是空的且没有任何提示** —— 根因是 `MessageBubble` 只读 `att.content`，而读路径上的消息按设计不带正文（`attachmentsFromMirror` 一律留空、按需取）⇒ 只有「刚上传那一刻」能看正文。修法：点开走 `getAttachmentContent(id)` + **有界重试**（4 次、退避 400/800/1200/1600ms、卸载清理定时器），读不到如实提示「再点一次可重试」。判据 `attachment-content-read.test.ts` 4 条。工具：`verify-attachment-visible-live.mjs`（挂附件→重启→量界面）、`verify-attachment-content-live.mjs`（一键跑完点击与采样）。**最终复核（1.16.140，装机版）**：点开后 **1 秒内正文出现**（指纹与库一致） |

| C-27 | **附件正文被「读回来再写回去」抹成 NULL**（静默数据丢失，1.16.139 修复的下一层） | 现场：附件条看得见但正文取不到 ⇒ 查库发现 `content` 是 **NULL**（`preview` 还在）。副本库确定性复现：带 `content` 键写回 ⇒ 变 null；不带该键 ⇒ 原值保留。根因：`writeAttachmentsViaPort`（启动期消息索引重建/回填/更新都走它）把 `content` 一起 upsert，而读路径上的消息按设计不带正文（`attachmentsFromMirror` 只投影元数据）⇒ `undefined` 被写成 NULL；**外置附件更严重**（`file:<路径>` 标记被抹掉后文件再也找不回）。修法：与引擎 `replace = 未提供的列保持原值` 对齐 —— 没有正文就不提供 `content`/`preview` 两列。判据 `attachment-content-keep.test.ts` 3 条；工具 `repro-attachment-content-wipe.mjs`（只写副本）。**装机版复核**：1.16.140 上挂附件 → 重启 → 点开 ⇒ 正文出现（**C-27 的修复正是「正文一直读不到」的最后一块**） |

| C-28 | **覆盖率没有按文件的地板**（O-6） | 聚合阈值挡不住「某个文件掉到 0、总量被别处补回来」。新增 `tools/audit/coverage-per-file.mjs` + `coverage-per-file-baseline.json`：地板 = 实测 × 0.8 向下取整（≥20 行才建；`--write` **只降不许升**），接进 `npm run verify`；判据 = 不得低于地板 + **新文件 0 覆盖且 ≥50 行必须拦** + 僵尸条目提示。**实测**：地板表 309 个文件，`--check` 全部通过。门禁 `coverage-per-file.test.ts` 5 条（合成数据测逻辑 + 两处变异 + 反向对照；真实数据那一步由 verify 里的 `--check` 负责 —— 写清分工是因为在 `vitest run --coverage` **进行中** summary 还没生成，第一版因此红过） |
| C-29 | **`contrast-checker` 写了却从没被用过**（0 覆盖 + 无调用方） | 盘点发现 `src/core/theme/contrast-checker.ts`（139 行，WCAG 2.1 对比度）**行覆盖 0%**，只在 `theme/index.ts` 被 re-export —— 文件头写着「用于验证三套皮肤」，但从来没跑过。新增 `skin-contrast.test.ts`：读 `src/styles.css` 的 `[data-theme=dark]` / `[data-theme=light]` 变量，用**被测实现**算对比度并卡线。实测：dark 12.95 / 11.55 / 8.85 / 7.89 / 5.42，light 16.50 / 15.79 / 7.37 / 7.06 / 5.25；primary 与 secondary 卡 **AAA ≥ 7.0**、muted 卡 **AA ≥ 4.5**。`hub` / `dream` 不覆盖这些变量（实测 0 个）⇒ 继承值同样被锁；将来若覆盖，CT-3 会要求为那套皮肤单独加判据 |

| C-30 | **内置 `skill-creator` 的 5 个 CLI 脚本从来没跑起来过**（第 97 轮实测发现并修，**仓库工具、不在装机包内**） | 判据是**真的去跑**（这是唯一能发现它的方式）：`npx tsx …/run-eval.ts --skill …` ⇒ `ReferenceError: require is not defined in ES module scope`。5 个脚本入口判定都是 CJS 的 `if (require.main === module)`，而 `package.json` 是 `"type": "module"`；`package-skill.ts` 还额外在模块顶层无条件 `main()`。`tsc`/`lint` 都不报（`@types/node` 声明了全局 `require`）。修法：新增 `scripts/is-main.ts`（`isMainModule(import.meta.url)`，win32 路径大小写不敏感），5 个脚本统一走它 + 兄弟 import 补 `.ts` 后缀 ⇒ `node <script>.ts`（Node ≥ 22.18）与 `npx tsx` 都能跑。门禁 `skill-creator-scripts.test.ts` **18 条**：CLI-1 用**真子进程**跑通校验与打包（zip 头 `PK`、无参数时印用法并 exit 1）、CLI-2 静态判据（全仓无 `require.main === module`、入口判定必须走 `isMainModule`、相对 import 必须带 `.ts`）、CLI-3 `import` 时不得执行 CLI 体；变异自证 **8/8**。**如实标注**：这 5 个脚本**不在装机包里**（`dist/` 无相关字符串、`tauri.conf.json` 无 `resources`），所以它是"仓库工具的坏"而不是"用户功能的坏"，本轮因此不出包 |
| C-31 | **死且坏的 `buildActivityTimeline` + 一处重复实现**（第 97 轮，零行为变化） | ①`src/core/llm/run-status-tracker.ts` 的 `buildActivityTimeline` + `ActivityItem`/`ActivityGroup` **全仓无引用**且读的字段在 `RunStatus` 上不存在（真接上就是 `undefined.map` 抛错）⇒ 删除 **-77 行**，两处指着它的陈旧注释改成当前事实（`src/App.tsx`、`src/styles/codem-ui.css`）；该文件现在只剩活着的纯函数，并由 RUN-1…6 覆盖。②`tools/ui-audit/icon-button-scan.mjs` 里那份 `buttonTags` 字符遍历与共用底层 `jsx-scan.mjs::findTags` 重复（第 95 轮抽底层时漏改，jscpd 一直报克隆）⇒ 改为复用。**等价性差分证明**：`.preview-shot/_diff-button-tags.mjs` 内联旧实现，对 **217 个生产 tsx / 937 个 button 标签**逐一比对 `{start,end,tag}` ⇒ 不一致 **0**，并用 `onClick={() => …}` 陷阱做反向对照；jscpd 克隆 **171 → 170** |

| C-32 | **「导入 Markdown」把每条笔记标题弄脏（U+FFFD）+ 把正文切碎丢掉**（第 98 轮，用户可见，1.16.144 出包） | ①标题：`parseNoteBlock` 用**没有 `u` 标志**的字符类正则剥 `📝`/`📊` 前缀 ⇒ 正则按 UTF-16 码元匹配，而 emoji 是代理对 ⇒ 只吃掉高位代理、留下孤立低位代理 ⇒ 标题变成 `"\uDCDD 要点"`（界面/库里就是一个替换字符）。改成 `(?:📝\|📊)` + `u` 标志。②结构：`splitSections` 原来把**任何** `## ` 行当新小节、笔记块按**任何** `### ` 切 ⇒ 笔记正文自带的 `## `/`### ` 会让正文落进未知小节被**整段丢掉**（只剩标题和时间戳）。改成：只认认得出的小节（摘要/来源/笔记），笔记块只认 `### 📝`/`### 📊` 记号（手写文件没有记号时退回老行为）。**判据**：`knowledge-export-import.test.ts` **17 条**（EXP/IMP/RT 三层，含 PPT 笔记不泄露正文、单块失败不拖垮其它块、非 Tauri 明确拒绝、正文里的标题不许被切碎）+ 变异自证 **9/9**（`.preview-shot/mutate-knowledge-import.mjs`）。**真机**：修复后的正则在运行中的 1.16.144 里取回核对（老形态不在），面板能开、控制台 0/0/0；**导入行为本身没点到**（原生文件选择框自动化不了，四套路子都试过，记录在钻取脚本头部） |
| C-33 | **正则里塞 emoji 却没加 `u` 标志**这类形态没有门禁（第 98 轮新增第 13 道 audit） | 新增 `tools/audit/scan-regex-unicode.mjs`：逐文件找**真正的正则字面量**（跳过字符串/模板/注释；`/` 只在"能开始表达式"的位置才算正则），报「`[...]` 里含码点 > U+FFFF 的字符且 flags 既无 `u` 也无 `v`」。已接进 `npm run audit`（`audit:regex-unicode`）。门禁 `regex-unicode-safety.test.ts` **4 条**：生产树 0 处（并自检扫描器没瞎：909 文件 / 1818 正则 / 1081 字符类）、**反向对照**（修复前那行必须被报出）、**误报对照**（emoji 数组 / 字符串里的 emoji / 带 `u` 的正则都不许报）、并直接复现该形态确实产生 U+FFFD。**顺带被自己的门禁抓了一次**：第一版把替换字符原样写进注释，UI 一致性门禁的 `encoding-replacement-char` 立刻报 error（源码里出现 U+FFFD = 编码损坏），已改成文字描述 |

| C-34 | **导入 PPTX 得到"空壳"**：真实 deck 一个字都读不到 + 图片永远丢（第 99 轮，用户可见，1.16.145 出包） | ①`parseShape` 里「拿不到 `<a:off>`/`<a:ext>` 就 `return null`」，而真实 PPTX 的**占位符几何继承自 slideLayout**（PowerPoint/python-pptx 写出来都是空 `<p:spPr/>`）⇒ 整块被丢，**每页元素 0、一个字都看不到，且不报错**（装机版 1.16.144 实测原文：`幻灯片 1 / 5 … 元素: 0`）。修法：缺几何时按占位符类型给默认版面（标题在上、正文占中下）。②图片填充查的是 `a:blipFill`，真实文件是 **`p:blipFill`** ⇒ 图片一律丢弃；已改成先查 `p:blipFill`（`a:blipFill` 兜底）。**判据**：`pptx-import-fixture.test.ts` **9 条**（页数与 zip 交叉核对 / 画布 EMU→px / 标题取 `docProps/core.xml` / 要点逐字对 / 图片解出的前 4 字节是 PNG 魔数 / 坐标百分比 / 坏输入抛可读错误 / **表格与备注读不到也写成断言**）+ 变异 **7/7**；夹具是 **python-pptx 1.0.2** 生成的第三方 deck（`tools/fixtures/make-pptx-fixture.py`，未改写）。**真机 before/after**（同一钻取、只动副本）：1.16.144 每页元素 0 ⇒ 1.16.145 第 2 页要点、第 4 页 data:image 图片都渲染出来，控制台 error 0 |
| C-35 | **测试环境（happy-dom）解不了真实 PPTX**：带前缀属性丢失 + 不认单引号 XML 声明（第 99 轮量清楚，不是产品缺陷） | 现象：`pptx-importer` 在 vitest 里对真实 deck 报 `No slides found in PPTX` / `PARSERERROR`。逐条量出根因：①happy-dom 解析 `<p:sldId r:id="rId2"/>` 后**只剩 `id`**，`getAttribute('r:id')` 返回 `null`（真实 PPTX 全靠它关联幻灯片）；②python-pptx 的 `<?xml version='1.0' …?>`（**单引号**，XML 规范允许）被 happy-dom 判成 `PARSERERROR`。**判定用对照**：在**运行中的装机版（真实 Chromium）**里跑同一段解析，两种声明、带前缀属性**都正常**（`.preview-shot/_probe-xml-decl-quotes.mjs` exit 0）⇒ 测试环境的毛病 ⇒ **换用 jsdom**（仓库已装 jsdom 29），而不是改产品迁就测试。全仓只有 `pptx-importer.ts` 用 `DOMParser` 解 XML，所以只影响这一个文件；变异 M3（去掉 `@vitest-environment jsdom`）会立刻红 |
| C-36 | **读侧失败被套进「写盘」通道**（第 100 轮，用户可见，1.16.146 出包） | 分诊上报点时发现 8 处把**读**事件送进 `persist`：提示条一律印「**写盘失败**……**本次改动只存在于内存，重启后可能丢失**」，而这些现场**什么都没有写**。逐处改：`chunk.onDemand`（端口在读取期间被换 / 文本块未读到）⇒ action；`chunk.onDemand`（块数超缓存预算 —— 读**成功了**，只是按预算不缓存）⇒ **advisory**（标题「知识库文本块超出缓存预算」+「这是容量上限而不是读取失败」）；`notebook.refreshCounts`（代价是"旧值原样保留"，与 persist 的默认后果句**恰好相反**）⇒ action；`todo.load`、`todo.updateStatus`（勾选没生效）、`fileChange.revert` ×2（回滚没执行）⇒ action。**判据**：`scan-report-sites --check`（登记与实际 kind 必须一致）+ `report-site-classification.test.ts` 的 pending 棘轮，变异 **8/8**（`.preview-shot/mutate-report-channels-r100.mjs`）。**真机**：新文案在运行中的 1.16.146 里取回核对；**如实标注**这 8 条的触发条件真机造不出来，语气由门禁守着 |
| C-37 | **「看板拖拽」从"无法验证"变成"量到了"**（第 101 轮，审计证据补齐，无代码改动） | 这一格此前只在走查清单里挂着"试不了"，因为看板用的是 **HTML5 DnD**（`draggable` + `dataTransfer`），鼠标事件序列不触发它。补上 CDP 的 `Input.setInterceptDrags`（拦到**真实载荷**，不是自造字符串）+ `Input.dispatchDragEvent` 派发 `dragEnter/dragOver/drop` 后，装机版实测：卡片刻度 **待办 → Backlog**、库里 `status` **todo → backlog**（跨面对照一致）、系统评论 **0 → 1**、拖回同列**无副作用**（评论 1 → 1）、控制台 error 0。脚本里同时记了两个踩过的坑：①直接扫全页 `[draggable="true"]` 会把**侧栏会话行**当看板卡片（第一版读出来的"卡片"是「对话 1/对话 2」）；②列要靠**结构**认（`display:flex` 且每个子元素都以状态标签开头），按"标签+高祖先"认会重复。越界自证：真库主库 sha256 一致、指纹真库 0 命中、副本阳性对照成立 |
| C-38 | **一对孤儿实现**：`llm/processor.ts` 的 `Processor` + `llm/session.ts` 的 `SessionManager`（第 102 轮删除，**非用户可见变更**） | 判据：全仓 `new Processor(` **0 处**、无按路径动态 import、无 barrel 再导出；`dist` 里连它的特征串都搜不到 ⇒ 早被 tree-shake（所以删不删都不改产物）。而 `session.ts` 的 `SessionManager` 类**唯一**调用方就是那个 `Processor`；`session.ts` 里还活着的是那批类型（6 处 `import type`）。处置：删 `processor.ts`（-270 行）、删 `SessionManager` 类与只服务于它的 3 个 import（-205 行）、删 `subagent.ts` 里那句**从未使用**的 `import type { ProcessorEvent }`（它让 knip 以为这个文件"有人用"），并把 `ARGS-6` 里对 `processor.ts` 的字符串断言改成"这个孤儿不许复活"。**量化收益**：零覆盖文件 **4 → 2**、knip **348/222 → 342/218**（已 `--update` 收紧）、jscpd 克隆 **169 → 167**、覆盖率地板条目 309 → 307（已清理僵尸条目）、全量用例仍 **392 文件 / 6192 通过 / 0 失败** |
| C-39 | **零覆盖清单清零**：`knowledge/graph-extractor.ts`（10 条）+ `knowledge/ppt-generator.ts`（13 条）（第 103 轮，**非用户可见变更**） | ①图谱抽取：GR-1 断言"索引未就绪 ⇒ 抛错，且 `deleteGraphData` **一次都没被调用**、LLM 一次都没被请求"（这正是"把读不到当成没有内容"会清空用户图谱的那个形态）；GR-6 钉住**跨批次 `chunkIndex` 换算**；GR-7/8 钉住部分失败可见与 60 块上限。②PPT 生成：五种失败文案各不相同且可区分（索引未就绪／没有内容／空响应／非 JSON／网络），**非网络错误原样抛**；来源筛选、60 块上限、**风格技能提示注入 systemPrompt** 都各自有判据；进度回调阶段有序。两个被测试抓出的口径：`styleId` 回填的是**规范化后**的 id（旧 id 经 `OLD_STYLE_ID_MAP` 映射），未知风格/画布 id 回退第一项（PPTG-12 钉住）。**量化收益**：≥50 行且 0 覆盖的生产文件 **2 → 0**；全局行覆盖 56.02% → **57.08%**；棘轮按实测上抬 branches 42→45、statements 50→53；全量用例 392 → **394 文件 / 6215 通过 / 0 失败** |
| C-40 | **上报语气三处修正 + 一句「我们重试过了」的假话**（第 104 轮，用户可见，1.16.147 出包） | ①`todo.updateStatus` 的两条分支（列表已不在库里 / `todos` 是坏 JSON）原来走 persist ⇒ 提示条印「写盘失败……本次改动只存在于内存」，而两处**什么都没写**（连目标行都没有）⇒ 改 action「操作失败」，与同文件"勾选的条目不在列表里"那条一致。②`eventProjection.compaction` 遇到形状不合契约的载荷时**投影没有失败**（按"不删除任何消息"降级处理并继续跑完，自检会在维护里报出）⇒ 从 persist 改 **advisory**（标题「压缩事件的形状不合契约」）。③**假话**：`sessionLog.rebuildIndex` 的 extra 一直写着「已回退旧路径重试」，那段回退**第 17 轮就删了**（同文件注释写明原因）⇒ 改成「本次重建**没有完成**（旧路径回退已在第 17 轮删除），下一次维护会再试」。**判据**：`scan-report-sites --check`（登记与实际 kind 必须一致）+ pending 棘轮（86 → **0**）；**真机**：从本机 dist 找出承载新文案的分包，再在**运行中的装机版**里 `fetch` 那份文件核对（新文案在、旧假话搜不到、控制台 0/0/0） |
| C-42 | **六批「延后注册」工具的注册竞态**（第 114 轮诊断、第 115 轮修、第 116 轮出包复核） | 现场：`src/core/llm/index.ts` 用 `import(spec).then(注册)` 注册 squad / issue / agent-teams / computer-use / 跨会话委派 / subagent-tools 六批工具，**没有任何等待点** ⇒ 刚启动就发第一条消息时工具表可能缺这几批（模型那一轮看不到它们）。证据不是猜的：`agent-teams/tools.ts` 的函数覆盖率在 **24.13%（7/29）** 与 **62.06%（18/29）** 之间跳（第 114 轮同一脚本 4 轮实测：24.13 🔴 / 62.06 ✅ ×3）—— 那次动态 import 有时跑完、有时没跑完。修法：新增 `LLMEngine.whenToolsReady()`（**再 import 一次同样的说明符**并 `await Promise.allSettled`；模块注册表返回同一个 promise，而我们的 `.then` 排在注册回调之后 ⇒ resolve 时注册一定已跑过），`process()` 构建请求前 await 它（已就绪时只多一个 microtask）。判据：`src/test/llm-tools-ready.test.ts` 4 条 + `agent-teams-tools.test.ts` 13 条（补齐后该文件 functions 连续读数 **62.06%**，地板余量 0 → +27 个百分点）。**装机版复核（1.16.149）**：更新器自升级；运行中的应用加载的 `main-BGrSViAf.js` 含 `whenToolsReady` 且 sha256 与本地构建产物逐字节一致；控制台 error/exception 0 |

> 编号说明：本表**没有 C-17**（早期跳号的历史遗留）。不重排编号，以免与已发布的 CHANGELOG / 文档里对 C-x 的引用错位。

## 四、**判定为"不是缺口"**的（附理由，避免反复被重新提起）

| 项 | 结论与理由 |
| --- | --- |
| 引擎 77 条命令里有 **35 条没有生产调用方** | **不是缺口**，是**引擎/CLI 的自省与迁移面**：`health` / `integrity_check` / `checkpoint` / `counts` / `audit.*` / `import.*` / `migration.*` / `digest.*` / `legacy.read_table` / `fts.delete_session` 等，使用者是 `codem-db-cli` 与 `tools/migrate/**`，不是渲染进程。工具：`node tools/audit/check-command-parity.mjs --list`（信息项，不判失败）。**判据是"有没有第二个写路径、列集合是否更窄"**，不是"有没有人调"—— `feedback.*` 三条正是因为**同时写同一张表且列集合更窄**才被删的，两者不要混为一谈 |
| `docs/` 下的历史分析文档与数字 | **不是缺口**：它们是各轮的取证记录（当时的数字、现场形态、判断依据），删掉等于把"当时为什么这么做"一起删掉。处置是加横幅 + 指回本文，而不是更新它们（更新历史记录=伪造记录） |
| `assets` 里的美术资源没有随源码版本走 | 见 `docs/ASSET-LICENSES.md`：许可与商用替代方案是刻意选择，不是遗漏 |

## 五、怎么维护本文

1. **只在有数字的时候**往"已关闭"里写。数字必须来自真实一次测量（测试套件、真机探针、审计工具输出），并写清"怎么核对的"。
2. 拿不准的写进"未关闭"，并写**关闭它的条件**（不是"以后再看"）。
3. 判定"不是缺口"的条目也要写理由 —— 不写理由的话，下一轮一定会有人重新提一遍。
4. 历史文档的横幅由 `.preview-shot/banner-historical-docs.mjs` 加（幂等），
   规则与守门用例 `src/test/docs-current-gap-list.test.ts` 一致：
   文件名匹配 `GAP|PLAN|ROADMAP|TODO|STATUS|TRIAGE|UNIMPLEMENTED|REMEDIATION|DEFERRED` 的
   `docs/*.md` **必须**带横幅，白名单只有本文。
