# 仓储迁移覆盖率（P2 门禁，自动生成）

> 由 `node tools/audit/storage-coverage.mjs --md` 生成；不要手工编辑。

- 扫描生产文件：**821**
- **旧库 SQL 调用点：0 —— 迁移已完成**（渲染侧不再有 `db.run`/`db.exec` 这类旧库调用；真源与引擎都在 Rust 侧）
- **端口调用点：268**（其中表名解析不出的 0 处）—— 渲染侧唯一的存储通路
- **存储调用点合计：268** —— 门禁的规模护栏盯这个和（只盘旧库会在迁移末期失去意义）
- 已实现：**0** → 方法覆盖率 **100%**，调用点覆盖率 **100%**
- Rust 侧已注册命令：**77**
- **旧库那一半已经清零**：`IMPLEMENTED`/`genericCommandFor` 这套映射服务的对象（旧库 SQL）已不存在，因此"命令可用性覆盖"这一项不再有意义 —— 现在要盯的是**端口侧调用点的规模与分布**（上面那两行）。

## 已实现（渲染侧方法 → Rust 命令）

| 渲染侧方法 | 调用点 | Rust 命令 |
|---|---:|---|

## 待迁移（0 个方法，按调用点排序）

| 渲染侧方法 | 调用点 | 建议阶段 |
|---|---:|---|
