# Codem — 项目功能说明介绍

> 一款面向「一个人就是一个研发团队」的桌面 AI 编程助手。
> 它不止是聊天框，而是把 **深度推理、多智能体协作、代码执行、文件与知识管理、生态扩展**
> 装进一个本地优先、开箱即用的桌面应用里——你提需求，它把想法变成可以运行的现实。

### 功能全景（13 大域 · 60+ 功能点）

| # | 功能域 | 功能点 | | # | 功能域 | 功能点 |
|---|---|---|---|---|---|---|
| 一 | 智能对话与深度推理 | 7 | | 八 | 图表、文档与演示 | 5 |
| 二 | 多智能体与团队协作 | 5 | | 九 | 插件与生态 | 4 |
| 三 | 代码执行与工程能力 | 6 | | 十 | 个性化与体验 | 5 |
| 四 | 搜索与知识 | 5 | | 十一 | 安全与信任 | 4 |
| 五 | 文件、工作区与审阅 | 4 | | 十二 | 连接与扩展生态 | 5 |
| 六 | 工作台与面板 | 5 | | 十三 | 质量与底座 | 3 |
| 七 | 自动化与任务 | 3 | | | **合计** | **61** |

---

## 一、智能对话与深度推理

- **流式思维可见**：回答逐字流出，思考过程（reasoning）可选展开——你既能看到结论，也能看清它「怎么想」的，放心把任务交给它。
- **深度推理与多模型自由切换**：接入 DeepSeek、MiMo 与一切 OpenAI 兼容 API；深度思考模式让复杂问题想得更深、更稳。
- **让纯文本模型也能「看见」**：内置视觉代理（Vision Proxy），给没有视觉能力的文本模型装上眼睛——贴一张截图、一张设计稿，它照样能读懂并执行。
- **上下文智能压缩，长会话不「失忆」**：对话窗口与压力驱动的自动压缩让超长任务也能稳步推进，回答始终引用有效上下文、不胡编。
- **消息编辑与回退**：一句话不合适？直接编辑后「回退重跑」，保留原会话、不破坏历史，迭代改进像聊天一样自然。
- **引用溯源**：回答中的每个结论都能点回来源文件与代码位置，证据链一目了然，不让你盲信。
- **任务步骤可视化**：计划、执行、验证以任务清单与步骤进度呈现——它做到哪一步、接下来做什么，全程透明可控。

## 二、多智能体与团队协作

- **多智能体并行作业**：一个主智能体带着多个子智能体分工并行——一个写代码、一个查资料、一个跑验证，人多力量大、速度翻倍。
- **一句话组建团队**：告诉它「建一个团队分别做 X/Y/Z 最后汇总」，它会按角色模板把团队搭起来、派活、收口——团队模板沉淀下来，随时复用。
- **任务中心可视化调度**：看板式任务中心让每个智能体的任务、状态、产出清清楚楚；谁在忙、谁完成了，一眼掌握。
- **执行轨迹全回放**：每一步工具调用、每一次思考都记录成可回放轨迹——出问题时像看监控回放一样定位，不再靠猜。
- **后台持续运行**：子智能体在后台干活、完成自动汇报；你可以同时推进别的事，它不打扰你。

## 三、代码执行与工程能力

- **全栈工具链**：读、写、改、搜、执行一手包办——代码文件、终端命令、正则检索都可信手调用，把「理解仓库」变成「动手改仓库」。
- **真实终端（PTY）**：交互式终端会话，支持长驻进程与实时流输出；跑测试、装依赖、起服务，和你在自己终端里操作一样。
- **代码结构智能理解**：语言服务器（LSP）+ 代码图谱自动接线——查定义、找引用、看调用关系，从「字符串搜索」升级为「结构理解」。
- **Git 全程护航**：分支创建/切换/合并入口、变更一键提交、工作树（worktree）并行开发、自动提交守护——版本管理顺手且安全。
- **电脑操作（computer-use）**：需要真动手的环节（点按钮、截屏确认、UI 走查）交给它，像人一样操作你的电脑，再汇报结果。
- **代码审查与质量门禁**：提交前自动预检、代码审查技能逐文件把关——把「上线前最担心的那几件事」提前挡在门外。

## 四、搜索与知识

- **极速精确检索**：内置 grep/glob 中文友好、即搜即得；大仓库也能快速定位精确标识符与引用。
- **语义级理解（zvec-grep）**：不知道准确措辞？用自然语言提问式检索——「主题偏好是怎么被保存和恢复的」直接找到相关实现与证据片段，像给代码库装了一个会思考的搜索引擎。
- **智能选路**：精确词你来找，语义模糊交给 AI 检索——同一句话它会自动选择最合适的方式，省 token 又准。
- **会话历史全文搜索**：几周前的结论、改过的文件，一句话全文搜回来，历史即资产。
- **知识管理三件套**：记事本沉淀项目笔记、记忆系统跨会话记住偏好、知识图谱把零散概念织成网——项目知识不再散落各处。

## 五、文件、工作区与审阅

- **文件树 + 全格式预览**：浏览项目文件，PDF、Excel、Word、图片、音视频、代码……点开即看，无需另开软件。
- **文件编辑器（悬浮全屏）**：需要专注改一个文件时，悬浮全屏编辑器即刻展开，边看边改。
- **变更追踪与内联 Diff 批量审阅**：改动一目了然，逐条 Diff 内联审批——同意就收、不同意就改，批量操作不再疲于弹窗。
- **工作区执行模式**：本地直接改 or 全新工作树副本——实验性改动不污染主工作区，试错成本降到最低。

## 六、工作台与面板

- **右侧栏多面板工作台**：Git 状态与提交、文件浏览、变更清单、工具工作台、CI/CD 面板——常用工程信息一栏收纳，随时可查可操作。
- **性能与用量仪表盘**：对话用量、Token 成本、缓存命中率（诚实精度）一目了然——花多少钱、省了多少，心里有数。
- **消息导航轨**：长对话也有「地图」——按内容比例分布的历史消息节点，滚动滑轨即可跳回任意关键节点，长对话不再迷路。
- **临时会话（Side Session）**：临时疑问单独开小窗问，不污染主对话主线；主次分明，专注不打断。

## 七、自动化与任务

- **目标驱动自动续行**：把大目标交给它，跨轮持续推进直到完成，不用每轮重复叮嘱。
- **定时提醒与自动化**：定时任务、条件触发，把重复性工作自动化——它像你的私人助理一样按点办事。
- **收件箱全局通知聚合**：智能体完成、任务到期、异常告警统一汇入收件箱，重要消息不遗漏。

## 八、图表、文档与演示

- **一张图讲清架构（archify）**：对着一堆代码说「画出系统架构/调用流程/数据流」，它输出**可交互、可导出、可验证来源**的精致 HTML 图——评审会、周报、新人 onboarding 都有料可讲。
- **Mermaid 与富图表**：流程图、时序图、状态机随手生成，也能把散落的 Mermaid 精修成专业级图。
- **专业 PPT 一键生成**：基于 74 种风格模板的演示文稿生成——把要讲的内容丢给它，成品幻灯片直接拿去讲。
- **知识可视化**：代码图谱、知识图谱让「仓库长什么样、概念怎么关联」看得见。

## 九、插件与生态

- **dsh 插件市场（50+ 官方条目）**：对标 DeepSeek Harness 生态，按兼容度诚实标注——内置等价能力一键启用、可适配协议插件即装即用；**不假装能装**，每一个按钮都真实可用。
- **技能（Skills）体系**：11 款内置技能开箱即用（代码审查、Prompt 优化、架构制图、Mermaid、润色规范……），还能从市场安装、自定义创作——能力像技能卡一样即插即用。
- **MCP 客户端**：任意 MCP 服务器（stdio/HTTP）接入，把外部能力（文件系统、数据库、浏览器、语义检索……）变成 AI 的工具。
- **开放扩展（可选运行时）**：需要 Node 生态的增强（如 zvec-grep 语义检索）也能以「用户按需安装的独立运行时包」接入——主程序保持轻量，能力却可无限生长。

## 十、个性化与体验

- **三种皮肤 + 毛玻璃质感**：默认、梦幻、Hub 三套主题，桌面级 Mica 毛玻璃与深/浅色自适应——好看，也护眼。
- **人设（Persona）与个性化**：给它一个人设，回答风格随之调整；头像、称呼、偏好皆可定制——它更像「你的」助手。
- **记忆与偏好**：常用技能、快捷指令、草稿、最近会话都在手边，常用的事三秒就位。
- **细节手感**：消息回退、精选 pin、思维展开、字号可调、拖拽排布……常用交互都被打磨到顺手为止。

## 十一、安全与信任

- **权限沙箱**：文件读写、命令执行按项目边界受控——不该碰的（.git、密钥、node_modules 之外的世界）它碰不到。
- **三种安全模式**：auto/ask/full 按你的信任程度切换；危险操作（强制推送、删库式命令）始终拦截确认。
- **关键操作人工审批**：写敏感文件、跑高危命令先弹窗请你确认——它替你干活，但方向盘始终在你手里。
- **隐私本地优先**：会话、文件、检索、模型（可选本地模型）默认全部留在本机；需要出网的能力（远端模型、语义索引）都由你显式授权。

## 十二、连接与扩展生态（外部世界）

- **微信 ClawBot 桥**：手机微信直接对话你的桌面 AI——出门在外也能派活、查进度，回复自动回到微信。
- **手机连接**：手机浏览器扫码配对，浏览/续聊/新建桌面会话——电脑没开也能用上它的能力。
- **桌面宠物**：干活时一只可爱状态卡宠物实时汇报「项目在跑什么、到哪一步了」——进度可见，等待不无聊。
- **内置彩蛋**：等它跑长任务时，来一局内置大富翁——工作娱乐两不误。

## 十三、质量与底座（为什么你可以信赖它）

- **本地优先的桌面应用**：Tauri v2（Rust + WebView2）原生手感，启动快、资源省、数据在你机器上。
- **两百余内置模块、数千回归用例守护**：每次交付都以全量自动化测试与编译零告警为门禁——它的「靠谱」不是口号，是被测试钉住的。
- **崩溃自愈与运行日志**：异常可诊断、日志可回放，出问题知道问题在哪，改起来有的放矢。

---

> **一句话总结**：Codem 想当的不是又一个聊天机器人，而是把「思考、执行、协作、记忆、生态」装进桌面、随时待命的一人研发团队——你负责判断方向，它负责把路走完。
