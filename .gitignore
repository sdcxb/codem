# Dependencies
node_modules/
dist/
dist-server/

# Tauri build artifacts
src-tauri/target/
# Rust 存储引擎 crate 的构建产物（crate 自身在 src-tauri/codem-db/ 下，独立于 Tauri workspace）
src-tauri/codem-db/target/
src-tauri/binaries/

# Tauri updater signing private key (NEVER commit)
.tauri/*.key
!.tauri/*.key.pub

# Logs
*.log
debug.log
engine.log
db-diag.log
load-msgs.log
session-switch.log

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Test output
test-output.json

# Reference projects (NOT part of Codem)
.deepseek-harness-ref/
.dsh-desktop-ref/
.wecode-ref/

# Internal analysis & training docs (not for public repo)
docs/codex-use-cases/
docs/training/

# Internal analysis & dev docs (keep only PROJECT-GUIDE.md, TODO.md, fonts)
docs/*.html
docs/*.png
docs/*.jpg
docs/*.md
docs/*.docx
!docs/PROJECT-GUIDE.md
!docs/ARCH-SQLITE-TO-RUST.md
!docs/STORAGE-INVENTORY.md
!docs/STORAGE-COVERAGE.md
!docs/DB-SCALE-BENCH.json
!docs/TODO.md
# 第 72/81 轮创建的两份**必须进仓库**的文档：
#   · GAP-LIST.md  —— 唯一"当前缺口清单"，门禁 `docs-current-gap-list.test.ts` 要求它存在；
#   · ui-walk-round72.md —— UI/UX 真机走查报告（GAP-LIST 与 CHANGELOG 都指向它）。
# 少了白名单，这两份会被 `docs/*.md` 吞掉：本机能跑、别人 clone 下来门禁直接失败，
# 而"清单在仓库里"这句话也就不成立了（这正是第 81 轮 `git ls-files` 查出来的）。
!docs/GAP-LIST.md
!docs/ui-walk-round72.md
!docs/AlimamaFangYuanTiVF-Thin.ttf
!docs/26720-1.png
!docs/26720-2.png
!docs/26720-3.png
!docs/7-6.png

!screenshots/

# Build logs & temp files
_build-log.txt
_dev-log.txt
build2.txt
tauri2.txt
notebook-section.txt

# GitHub workflows from reference project
.github/

# Reference project (not part of codebase)
.oh-my-ppt-ref/

# Agent tooling runtime artifacts
.codem-cache/
.codem-snapshots/
.codem-tool-results/

# EAC reference repo (analysis only)
.eac-ref/


# EAC analysis artifacts (temporary)
.eac-analysis/

# Library Ops plugin docs（交付物，随插件一起发布）
!docs/LIBRARY-OPS-PLUGIN.md
!docs/LIBRARY-OPS-AUDIT.md
# 侧栏外观对标审计（第 166 轮）：与前几轮的「令牌对比」不同，这份量的是渲染结果与交互状态；
# 结论直接决定 UI 路线的下一步，需要进仓库（否则只在本机存在，别人 clone 看不到）。
!docs/SIDEBAR-AUDIT-OPENBITFUN.md
# UI 优化方案（第 166 轮）：P0/P1/P2 三批、每项带可机证验收指标与门禁要求。
!docs/UI-OPTIMIZATION-SPEC.md
!docs/TASK-CENTER-MAP.md
!docs/UI-DESIGN-SYSTEM.md
!docs/ASSET-LICENSES.md
!docs/ASSET-PROMPT-PACK.md
!docs/art-prompts/*.md

# 美术素材投放目录（用户放生成图用，内容不入库）
.art-inbox/*
!.art-inbox/README.txt

# 视觉预览 / DOM 审计产物（tools/preview 的源码保留，产物忽略）
.preview-shot/

# ============================================================================
# 运行时数据库与备份 —— **必须忽略**（第 55 轮真事故）
# ============================================================================
# 为什么必须有这一条：从仓库目录启动应用时，引擎的库文件就落在**工作目录**里
# （`codem-db-rust.bin` + `-wal` / `-shm`）。这些文件里是**用户的真实数据**：
# 全部对话正文、事件、附件元数据。
#
# 真事故（2026-09-18）：一次 `git add -A` 把 `codem-db-rust.bin` / `-wal` / `-shm`
# 一起提交了；推送时被 GitHub 的 push protection 拦下 —— 因为库里某条**消息正文**
# 含有一个长得像 GitHub OAuth token 的字符串（用户自己贴进对话的），
# 于是"提交用户数据库"差点变成"把用户的凭据发到公开仓库"。
# 拦下是运气好（push protection），不是设计使然；这条规则才是设计。
#
# 教训：**数据目录可能等于工作目录**。凡是"用户数据落到磁盘上的产物"都要在这里点名。
codem-db-rust.bin
codem-db-rust.bin-wal
codem-db-rust.bin-shm
codem-db-rust.bin.corrupt-*
codem-db-rust.bin.pre-*
codem-db.bin
codem-db.bin-wal
codem-db.bin-shm
codem-db.bin.pre-*
codem-db.bin.corrupt-*
# 会话权威日志（JSONL）与运行时日志：同样是用户数据
# ⚠️ 只锚定**仓库根**（`/sessions/`）：数据目录解析失败时引擎会把它们落在工作目录，
# 而工作目录就是仓库根。锚定根目录可以避免误伤将来任何叫 `sessions/` 的源码目录。
/sessions/
codem-runtime-*.log



# 覆盖率与重复率报告（npm run verify 的产物，体积大且每次重新生成）
/coverage/
/jscpd-report/
